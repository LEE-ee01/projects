# 数据开发核心技术知识讲解

> 面向岗位：启明星辰 — 数据开发  
> 结合项目：网络水军检测 / gnucash-web / 农富坊

---

## 第一部分：SQL 核心（面试必考，重中之重）

### 1.1 执行顺序（理解 SQL 的底层逻辑）

SQL 的**书写顺序**和**执行顺序**不同，面试常考：

`
书写顺序：SELECT → FROM → JOIN → WHERE → GROUP BY → HAVING → ORDER BY → LIMIT
执行顺序：FROM → JOIN → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT
`

**为什么重要？**
- 别名只能在 SELECT 及之后的步骤使用，所以 WHERE 里不能用 SELECT 中定义的别名
- 聚合函数（SUM/MAX/COUNT）在 HAVING 里可用，在 WHERE 里不可用（因为 WHERE 执行时还没分组）

### 1.2 窗口函数（Window Function）—— 面试高频考点

窗口函数是数据开发面试的**分水岭**，必须熟练掌握。

**核心语法：**
`sql
函数名() OVER (
    [PARTITION BY 分区列]
    [ORDER BY 排序列]
    [ROWS/RANGE 窗口范围]
) AS 别名
`

**三大类窗口函数：**

| 类型 | 函数 | 作用 |
|------|------|------|
| 排序类 | ROW_NUMBER() / RANK() / DENSE_RANK() | 给行编号，去重/TopN 必备 |
| 聚合类 | SUM() / AVG() / COUNT() / MAX() / MIN() | 滑动聚合，累计计算 |
| 偏移类 | LAG() / LEAD() / FIRST_VALUE() / LAST_VALUE() | 取前后行的值，环比/同比必备 |

**经典场景与代码：**

**场景1：每个用户最近一次订单（TopN 问题）**
`sql
SELECT user_id, order_amount
FROM (
    SELECT user_id, order_amount,
           ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY order_date DESC) as rn
    FROM orders
) t
WHERE rn = 1;
`

**场景2：连续3天登录的用户（连续问题）**
`sql
-- 核心思路：日期减去排序序号，连续日期的差值相同
SELECT DISTINCT user_id
FROM (
    SELECT user_id,
           DATE_SUB(login_date, INTERVAL 
               ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY login_date) DAY
           ) as grp
    FROM user_logins
) t
GROUP BY user_id, grp
HAVING COUNT(*) >= 3;
`

**场景3：每月销售额及累计销售额（累计求和）**
`sql
SELECT 
    month,
    monthly_amount,
    SUM(monthly_amount) OVER (ORDER BY month) as cum_amount  -- 累计和
FROM monthly_sales;
`

**场景4：同比环比（LAG/LEAD）**
`sql
SELECT 
    month,
    amount,
    LAG(amount, 1) OVER (ORDER BY month) as last_month,     -- 上月
    LAG(amount, 12) OVER (ORDER BY month) as last_year       -- 去年同期
FROM sales;
`

**ROW_NUMBER vs RANK vs DENSE_RANK 区别：**
- ROW_NUMBER()：1,2,3,4... 无重复，去重首选
- RANK()：1,1,3,4... 有重复会跳号
- DENSE_RANK()：1,1,2,3... 有重复不跳号

### 1.3 JOIN 的底层原理

`sql
-- INNER JOIN：只保留两表匹配的记录
-- LEFT JOIN：保留左表全部，右表无匹配填 NULL
-- RIGHT JOIN：保留右表全部
-- FULL OUTER JOIN：保留两表全部（MySQL 不支持，可用 UNION 模拟）
-- CROSS JOIN：笛卡尔积
`

**面试陷阱题：LEFT JOIN 后 WHERE 过滤 vs ON 过滤的区别？**
`sql
-- 写法A：ON 里过滤（结果可能包含右表为 NULL 的行）
SELECT * FROM A LEFT JOIN B ON A.id = B.id AND B.status = 1;

-- 写法B：WHERE 里过滤（会把右表为 NULL 的行也过滤掉，变成 INNER JOIN 效果）
SELECT * FROM A LEFT JOIN B ON A.id = B.id WHERE B.status = 1;
`

### 1.4 性能优化基础

**索引原理（B+树）：**
- 索引是排序后的数据结构，查询时从根节点二分查找，时间复杂度 O(logN)
- 聚簇索引：数据和索引在一起（InnoDB 主键索引）
- 非聚簇索引：索引和数据分离，需要"回表"查询
- **最左前缀原则**：联合索引 (a,b,c) 只能用于 a / a,b / a,b,c 的查询

**SQL 优化 checklist：**
1. SELECT 只取需要的列，避免 SELECT *
2. WHERE 条件中对索引列不要做函数运算（如 DATE(create_time) = '2024-01-01' 会导致索引失效）
3. 大表分页用 "延迟关联" 或 "游标分页"，避免深分页 LIMIT 100000, 10
4. 小表驱动大表：IN 适合子查询结果小的情况，EXISTS 适合子查询结果大的情况

---

## 第二部分：数据仓库与 ETL

### 2.1 数据仓库核心概念

**数据仓库（DW）vs 数据库（DB）的区别：**

| 维度 | 数据库（OLTP） | 数据仓库（OLAP） |
|------|---------------|-----------------|
| 用途 | 业务交易处理 | 分析决策支持 |
| 数据特征 | 当前最新、细节数据 | 历史累积、汇总数据 |
| 读写模式 | 频繁增删改查 | 批量写入、大量查询 |
| 设计范式 | 3NF（减少冗余） | 维度建模（适当冗余换取查询速度） |

**数据仓库分层（经典架构）：**

`
┌─────────────────────────────────────────────┐
│  ADS（应用数据层）-- 面向具体应用场景的宽表      │
├─────────────────────────────────────────────┤
│  DWS（汇总数据层）-- 按主题轻度汇总             │
├─────────────────────────────────────────────┤
│  DWD（明细数据层）-- 清洗后的业务事实明细        │
├─────────────────────────────────────────────┤
│  ODS（贴源数据层）-- 原始数据，结构与源系统一致   │
└─────────────────────────────────────────────┘
`

**各层作用一句话总结：**
- **ODS**：数据"原封不动"地从业务系统同步过来，作为备份和溯源
- **DWD**：对 ODS 做数据清洗（去重、空值处理、格式统一）、维度退化，形成干净的明细事实表
- **DWS**：按业务主题（用户、商品、订单）做轻度聚合，形成主题宽表
- **ADS**：面向具体应用（报表、BI、算法），直接可用的结果表

### 2.2 ETL 流程详解

**ETL = Extract（抽取）+ Transform（转换）+ Load（加载）**

`
业务系统（MySQL/Oracle/API）
    │
    ▼
[Extract]  全量抽取 / 增量抽取（按时间戳/自增ID/CDC）
    │
    ▼
[Transform] 数据清洗 → 格式转换 → 业务计算 → 关联整合
    │
    ▼
[Load] 写入目标系统（数据仓库/数据湖/API）
`

**增量抽取的三种方式：**

| 方式 | 原理 | 适用场景 | 缺点 |
|------|------|---------|------|
| 时间戳 | 记录最后同步时间，只取 update_time > last_sync | 有更新时间字段的表 | 删除的数据无法感知 |
| 自增ID | 记录最大ID，只取 id > max_id | 有自增主键的表 | 删除、更新无法感知 |
| CDC（变更数据捕获）| 监听数据库 binlog，捕获所有变更 | 实时性要求高的场景 | 实现复杂，需要中间件（如 Canal/Debezium） |

**面试话术（结合你的项目）：**
> 在我的网络水军检测项目中，虽然没有使用传统 ETL 工具（如 Kettle/Informatica），但我独立实现了一套完整的 ETL 流程：从 Excel 原始数据 **Extract** 读取 → **Transform** 阶段进行分词、特征计算（文本统计、用户行为聚合、时间维度转换）、标准化处理 → **Load** 阶段输出为特征宽表（CSV 格式）供下游模型使用。这个过程让我深刻理解了 ETL 的核心逻辑：数据质量的保障、转换规则的可复用性、以及上下游的依赖管理。

### 2.3 数据质量

**数据质量六大维度：**
1. **完整性**：字段是否都有值？（空值率）
2. **准确性**：数据是否符合业务规则？（数值范围、枚举值）
3. **一致性**：同一数据在不同系统是否一致？
4. **及时性**：数据是否按时到达？
5. **唯一性**：主键是否唯一？有无重复记录？
6. **有效性**：数据格式是否正确？（手机号、邮箱正则校验）

---

## 第三部分：数据建模

### 3.1 维度建模（Kimball 方法论）

数据仓库领域最主流的建模方法论，由 Ralph Kimball 提出。

**核心概念：**
- **事实表（Fact Table）**：存储业务的度量数据（可累加的数值），如订单金额、数量
- **维度表（Dimension Table）**：存储业务实体的描述信息，如用户、商品、时间
- **主键-外键关联**：事实表通过外键关联维度表

**两种模型对比：**

| 特征 | 星型模型 | 雪花模型 |
|------|---------|---------|
| 结构 | 事实表直接关联维度表 | 维度表进一步规范化 |
| 冗余度 | 高（维度信息直接冗余在维度表） | 低 |
| 查询复杂度 | 简单（关联少） | 复杂（多层关联） |
| 查询性能 | 快 | 稍慢 |
| 维护成本 | 低 | 高 |
| 适用场景 | 大多数 OLAP 场景 | 维度属性极多、需要强一致性 |

**星型模型示意图：**
`
                    ┌─────────────┐
                    │  dim_date   │
                    └──────┬──────┘
                           │
    ┌─────────────┐   ┌────┴────┐   ┌─────────────┐
    │  dim_user   │───┤ fact_   ├───│  dim_product │
    └─────────────┘   │ order   │   └─────────────┘
                      └────┬────┘
                           │
                    ┌──────┴──────┐
                    │ dim_region  │
                    └─────────────┘
`

**缓慢变化维（SCD）—— 面试常考：**

维度表的数据会随时间变化（如用户修改地址），如何处理？

| 类型 | 名称 | 做法 | 适用场景 |
|------|------|------|---------|
| SCD Type 1 | 直接覆盖 | 旧值直接被新值覆盖 | 不关心历史，只关心当前 |
| SCD Type 2 | 增加版本 | 新增一行，保留旧行，加生效/失效时间戳 | 需要完整历史追溯 |
| SCD Type 3 | 增加列 | 增加"旧值"列，只保留上一版 | 只需追溯最近一期 |

> 实际工作中 **Type 2 最常用**，因为它能保留完整历史且实现相对简单。

### 3.2 结合你的项目谈建模

**网络水军项目中的"图建模"如何转化为数仓语言？**

你的二部图模型可以这样映射：
- **事实表**：act_comment（用户ID、商品ID、评论内容、评分、评论时间）
- **维度表**：dim_user（用户ID、注册时间、等级）、dim_product（商品ID、类目、价格带）
- 二部图的分析 = 事实表关联两个维度表后，用图算法（PageRank、Louvain）在聚合层做分析

**面试话术：**
> 我在网络水军项目中用图网络分析用户和商品的关系，这个思路其实和数仓的维度建模是相通的——用户和商品就是两个维度实体，评论行为是事实数据。不同之处在于，传统数仓用 GROUP BY 做聚合统计，而图算法（如 Louvain 社区检测）能发现更隐蔽的团伙模式。

---

## 第四部分：大数据组件速通（Kafka / Flink / Hive）

### 4.1 Kafka（分布式消息队列）

**核心定位**：高吞吐量的分布式发布-订阅消息系统，常用于实时数仓的**数据采集层**。

**核心概念：**

| 概念 | 解释 | 类比 |
|------|------|------|
| Producer | 生产者，向 Kafka 发送消息 | 快递员 |
| Consumer | 消费者，从 Kafka 读取消息 | 收件人 |
| Topic | 消息主题，逻辑分类 | 快递柜的格子（按小区分） |
| Partition | 主题的分区，物理分片 | 格子里的多个隔间 |
| Offset | 消息在分区中的位置序号 | 隔间里的编号 |
| Broker | Kafka 服务器节点 | 快递柜 |

**Kafka 在实时数仓中的角色：**
`
业务系统日志 → [Producer] → Kafka Topic → [Consumer] → Flink/Spark → 数仓
`

**面试必备知识点：**
- **为什么用 Kafka 而不是直接写数据库？**
  - 解耦：生产者和消费者互不影响
  - 削峰：高峰期消息暂存，消费者按能力消费
  - 高吞吐：单机可达数十万 QPS
  - 可回溯：通过 Offset 可以重新消费历史数据

- **如何保证消息不丢失？**
  - Producer：设置 cks=all（等所有副本确认）
  - Broker：设置副本数 eplication.factor >= 2
  - Consumer：手动提交 Offset，处理完业务再提交

- **如何保证消息顺序？**
  - 同一 Partition 内有序，不同 Partition 无序
  - 需要全局有序时，把相关消息发到同一个 Partition（按 Key Hash）

### 4.2 Flink（实时流处理引擎）

**核心定位**：真正的**流处理**框架（对比 Spark Streaming 的微批处理），毫秒级延迟。

**Flink vs Spark Streaming：**

| 特性 | Flink | Spark Streaming |
|------|-------|-----------------|
| 处理模式 | 原生流处理（逐条处理） | 微批处理（攒一小批再处理） |
| 延迟 | 毫秒级 | 秒级 |
| 语义 | 精确一次（Exactly-Once） | 精确一次（较难实现） |
| 状态管理 | 内置状态后端（RocksDB/Memory） | 依赖外部存储 |
| 容错 | Checkpoint 机制 | RDD 血缘 + Checkpoint |
| 适用场景 | 实时监控、风控、推荐 | 准实时统计、日志分析 |

**Flink 核心概念：**
- **DataStream API**：流处理编程接口（支持 Java/Scala/Python）
- **Event Time / Processing Time / Ingestion Time**：三种时间语义
  - Event Time：事件实际发生的时间（推荐，能处理乱序数据）
  - Processing Time：数据被处理的时间（最简单，但不准确）
- **Watermark（水位线）**：用来衡量 Event Time 的进度，允许一定时间的乱序数据延迟到达
- **Window（窗口）**：对无界流数据进行切分
  - 滚动窗口（Tumbling Window）：固定大小、不重叠
  - 滑动窗口（Sliding Window）：固定大小、可重叠
  - 会话窗口（Session Window）：按活动间隙划分
- **Checkpoint & Savepoint**：容错机制，定期保存状态快照

**Flink 在实时数仓中的角色：**
`
Kafka → Flink（清洗/转换/窗口聚合）→ 写入数仓（HBase/Doris/ClickHouse）
`

**面试话术（坦诚但有准备）：**
> 我目前对 Flink 的理解主要来自理论学习，还没有生产环境的实战经验。但我知道 Flink 的核心优势是原生流处理和精确一次语义，在实时数仓中通常扮演"从 Kafka 消费数据 → 做实时清洗和窗口聚合 → 写入 OLAP 引擎"的角色。如果公司使用 Flink，我可以快速上手——它的 DataStream API 思想和 pandas 的数据转换逻辑是相通的，只是换成了流式语义。

### 4.3 Hive（基于 Hadoop 的数据仓库工具）

**核心定位**：把 HDFS 上的文件映射成表，让用户可以用 **SQL（HiveQL）** 来分析海量数据。

**Hive 的本质：**
> Hive 不是数据库，而是一个**翻译器**——把 HiveQL 翻译成 MapReduce/Tez/Spark 任务来执行。

**Hive 核心特性：**

| 特性 | 说明 |
|------|------|
| 分区（Partition）| 按某个列的值（通常是日期）把数据分到不同目录，查询时只扫描相关目录 |
| 分桶（Bucket）| 按 Hash 把数据分到固定数量的文件中，优化 Join 和采样 |
| 存储格式 | TEXTFILE（纯文本）、ORC（列式存储，推荐）、PARQUET（列式存储） |
| 压缩 | Snappy、Zlib 等，减少存储和传输开销 |

**分区 vs 分桶 的区别：**
- **分区**：按目录切分，适合离散值（如日期 dt='2024-01-01'）
- **分桶**：按文件切分，适合连续值或 Join Key（如 user_id）

**Hive SQL 与标准 SQL 的差异：**
`sql
-- 1. 分区查询必须带分区条件（否则会全表扫描）
SELECT * FROM orders WHERE dt = '2024-01-01';

-- 2. 支持 LATERAL VIEW 展开复杂类型（ARRAY/MAP）
LATERAL VIEW explode(interests) t AS interest;

-- 3. 支持 DISTRIBUTE BY / SORT BY（控制 MapReduce 的分布和排序）
SELECT * FROM t DISTRIBUTE BY user_id SORT BY create_time;

-- 4. 窗口函数支持完整（和 MySQL 8.0+ 基本一致）
`

**Hive 优化要点：**
1. 分区裁剪：WHERE 条件带分区字段，避免全表扫描
2. ORC + Snappy：列式存储 + 压缩，查询快、存储省
3. 小文件合并：MapReduce 任务产生大量小文件会影响后续查询性能
4. MapJoin：小表放内存里广播，避免 Shuffle

---

## 第五部分：网络水军项目中的技术原理详解

### 5.1 特征工程原理

**你在项目中做了哪些特征？对应什么业务含义？**

| 特征类别 | 具体特征 | 业务含义 |
|---------|---------|---------|
| 文本特征 | 文本长度、词数、标点占比、重复字符率 | 水军评论通常短、模板化、感叹号多 |
| 用户行为 | 评论频率、评分均值/方差、时间间隔 | 水军账号通常高频、评分极端、批量发布 |
| 时间特征 | 小时、星期、是否夜间、是否周末 | 水军可能在非正常时段批量作业 |
| 评分特征 | 评分值、极端评分标记 | 水军通常全5星或全1星 |

**TF-IDF 原理（简要版）：**
> TF-IDF 衡量一个词在文档中的重要性。TF（词频）= 词在文档中出现次数 / 文档总词数；IDF（逆文档频率）= log(总文档数 / 包含该词的文档数)。如果一个词在某文档中出现多、但在其他文档中很少出现，则 TF-IDF 值高，说明这个词对该文档具有区分度。

### 5.2 图网络算法原理

**PageRank 原理（一句话）：**
> 一个节点的重要性，取决于指向它的其他节点的重要性之和。被重要节点指向的节点，自己也很重要。Google 最早用它来给网页排序。

**Louvain 社区检测原理（一句话）：**
> 通过最大化模块度（Modularity）来发现社区。模块度衡量"社区内部连接紧密程度"与"随机连接期望"的差距。Louvain 算法分两个阶段迭代：① 把每个节点当成独立社区，尝试合并到邻居社区看模块度是否增加；② 把发现的社区压缩成超节点，重新构建网络。反复执行直到模块度不再增加。

**中心性指标含义：**

| 指标 | 含义 | 水军检测中的意义 |
|------|------|----------------|
| 度中心性 | 连接数越多越重要 | 批量评论多个商品的用户 |
| 介数中心性 | 作为桥梁的次数越多越重要 | 连接不同水军团伙的核心账号 |
| 特征向量中心性 | 被重要节点指向的节点重要 | 团伙头目通常互相连接 |

### 5.3 集成学习原理

**为什么用集成学习？**
> 单一模型容易过拟合或欠拟合，集成学习通过"三个臭皮匠顶个诸葛亮"的思想，组合多个模型的预测结果，降低方差和偏差。

**Bagging vs Boosting vs Stacking：**

| 方法 | 代表算法 | 原理 | 特点 |
|------|---------|------|------|
| Bagging | RandomForest | 并行训练多个模型，结果投票/平均 | 降低方差，抗过拟合 |
| Boosting | XGBoost / LightGBM / CatBoost | 串行训练，每轮关注前轮的错分样本 | 降低偏差，精度高 |
| Stacking | 自定义 | 用多个模型的输出作为新特征，再训练一个元模型 | 融合能力强，容易过拟合 |

**SMOTE 原理（处理类别不平衡）：**
> SMOTE（Synthetic Minority Over-sampling Technique）对少数类样本，在其 k 近邻之间随机插值生成新样本。例如：少数类样本 A 的 5 个近邻是 B/C/D/E/F，随机选 B，在 A 和 B 的连线上随机取一点作为新样本。这样可以扩充少数类，同时避免简单复制导致的过拟合。

**XGBoost 核心优势（面试高频）：**
1. 正则化（L1/L2）：防止过拟合
2. 二阶泰勒展开：损失函数更精确
3. 缺失值处理：自动学习缺失值的分裂方向
4. 并行计算：特征级别的并行分裂点搜索
5. 列采样：防止过拟合，类似 RandomForest

**LightGBM vs XGBoost：**
- LightGBM 使用 **Leaf-wise** 分裂（每次选损失下降最大的叶子分裂），XGBoost 使用 **Level-wise**（按层分裂）
- LightGBM 采用 **GOSS（Gradient-based One-Side Sampling）** 和 **EFB（Exclusive Feature Bundling）** 加速，训练速度更快、内存占用更少
- 大数据场景下 LightGBM 通常更优

---

## 第六部分：Java 后端与前后端协作（gnucash-web 相关）

### 6.1 RESTful API 设计规范

| HTTP 方法 | 含义 | 示例 |
|-----------|------|------|
| GET | 获取资源 | GET /accounts 获取科目列表 |
| POST | 创建资源 | POST /transactions 新增凭证 |
| PUT | 全量更新 | PUT /accounts/1001 更新科目1001 |
| PATCH | 部分更新 | PATCH /accounts/1001 更新部分字段 |
| DELETE | 删除资源 | DELETE /accounts/1001 |

**HTTP 状态码：**
- 200 OK：成功
- 201 Created：创建成功
- 400 Bad Request：请求参数错误
- 401 Unauthorized：未认证
- 403 Forbidden：无权限
- 404 Not Found：资源不存在
- 500 Internal Server Error：服务器内部错误

### 6.2 Axios 拦截器原理

你在 gnucash-web 中封装了请求拦截器，核心逻辑：
`js
service.interceptors.request.use(
  config => {
    // 在请求发送前统一处理
    const bookId = localStorage.getItem('currentBookId');
    if (bookId) config.headers['X-Book-Id'] = bookId;
    return config;
  },
  error => Promise.reject(error)
);
`

**面试话术：**
> 我在 gnucash-web 中封装了 Axios 请求拦截器，实现了多账簿隔离——每次请求自动注入当前账簿 ID。这让我理解了前后端协作中"统一入口处理通用逻辑"的重要性，这个思想和数据开发中"ETL 流程的统一元数据管理"是相通的。

---

## 附录：面试快速记忆卡片

### 卡片1：SQL 窗口函数速查
`
ROW_NUMBER() OVER (PARTITION BY ... ORDER BY ...)  → 去重/TopN
RANK() / DENSE_RANK()                             → 排名
SUM/AVG/COUNT() OVER (...)                         → 累计/滑动聚合
LAG/LEAD(col, n) OVER (...)                        → 同比环比
`

### 卡片2：数仓分层速记
`
ODS：原始数据，原封不动
DWD：清洗明细，事实表
DWS：轻度聚合，主题宽表
ADS：应用结果，直接可用
`

### 卡片3：Kafka 核心速记
`
Producer → Topic(Partition) → Consumer
Offset = 消费位置
acks=all + 多副本 = 不丢失
同 Partition 有序，不同 Partition 无序
`

### 卡片4：Flink 核心速记
`
Event Time + Watermark = 处理乱序
Window = 切分流数据
Checkpoint = 容错快照
原生流处理，毫秒级延迟
`

### 卡片5：Hive 核心速记
`
Hive ≠ 数据库，是 SQL→MapReduce 的翻译器
分区 = 按目录切（适合日期）
分桶 = 按文件切（适合 Join）
ORC + Snappy = 查询快 + 存储省
`

---

> **建议学习顺序**：
> 1. SQL 窗口函数（必须手写练习）
> 2. 数据仓库分层 + ETL 流程（理解概念即可）
> 3. 维度建模 + 星型模型（能画出来、讲出来）
> 4. Kafka / Flink / Hive（理解定位 + 核心概念，不纠结细节）
> 5. 项目技术点（BERT/GNN/PageRank/XGBoost 等，能讲清楚"是什么、为什么、效果怎么样"）