# 数据开发面试背诵全书

> **使用说明**：⭐标记为必须逐字背诵的内容；其余理解后可用自己的话复述。  
> 目标岗位：启明星辰 — 数据开发  
> 个人背景：商学院 → 技术方向，项目经验 = 网络水军检测 + gnucash-web + 农富坊大创

---

# 第一篇：自我介绍与项目话术

---

## ⭐ 1.1 自我介绍 —— 1分30秒版（逐字背诵）

> 面试官您好，我叫李欣昱，应聘数据开发岗位。
>
> 我从三个方面介绍自己：
>
> **第一，我有完整的数据 Pipeline 实践经验。** 在网络水军检测项目中，我独立完成了从数据采集、清洗、特征工程、图网络建模、集成学习到最终报告生成的全流程。这个流程本质上就是数据开发中的 ETL——从原始数据抽取、转换、加载为结构化特征表，只是用的工具是 Python + pandas 而不是传统的 Kettle 或 Informatica。最终模型的 F1 分数从 0.53 提升到了 0.84，准确率达到 95.7%。
>
> **第二，我有数据建模的思维习惯。** 在网络水军项目中，我构建了用户-商品二部图模型来发现水军团伙，用 PageRank 和 Louvain 社区检测做团伙分析；在财务系统项目中，我设计了会计科目的树形层级模型。我能根据业务需求抽象出数据模型，并落地为可计算、可查询的数据结构。
>
> **第三，我学习能力很强。** 为了把水军检测效果做好，我主动学习了 BERT 预训练模型和图神经网络，并成功把它们融入方案。BERT 特征的加入让 F1 直接提升了 45%。对于我目前没有实战经验的大数据组件，比如 Kafka、Flink、Hive，我已经系统学习了它们的工作原理，也做好了入职后快速上手的准备。
>
> 我非常希望能在启明星辰的数据开发团队中成长，谢谢！

---

## ⭐ 1.2 自我介绍 —— 45秒极速版（逐字背诵）

> 面试官您好，我叫李欣昱，应聘数据开发。
>
> 我有三个核心亮点：第一，在网络水军检测项目中，我独立完成了一整套数据处理 Pipeline，F1 从 0.53 提升到 0.84；第二，我擅长数据建模，从业务需求出发设计数据结构，无论是图模型还是树形模型都能落地；第三，我学习能力强，从零学了 BERT 和 GNN 并成功应用到项目中。
>
> 虽然大数据组件实战经验不多，但核心的 ETL、SQL、数据建模我都深入实践过，能快速补齐技术栈。

---

## ⭐ 1.3 项目介绍 —— 3分钟标准版（逐字背诵）

> 我的核心项目是**电商水军评论检测系统**，数据量是 12 万条评论。
>
> 背景是电商平台上有大量水军刷虚假评论，正常用户和水军的比例大约是 10:1，属于严重的类别不平衡问题。
>
> 整个项目分为 8 个步骤。
>
> **第一步是特征工程。** 我从文本、用户行为、时间、评分四个维度，提取了 20 多个特征。文本方面包括评论长度、词数、标点占比、重复字符率，用了 jieba 做中文分词和停用词过滤；用户行为方面包括评论频率、评分均值和方差、时间间隔；时间特征包括几点评的、是不是周末、是不是深夜；评分特征包括是否极端评分。这一步本质上就是 ETL 中的 Transform 环节。
>
> **第二步是无监督异常检测。** 用 Isolation Forest、DBSCAN、LOF 三种方法分别找异常，然后投票——三个方法里至少两个认为是异常的，才标记为可疑。这解决的是"标注数据可能不全"的问题。
>
> **第三步是图网络分析。** 我构建了两张图：一张是用户-商品二部图，连接谁评论了哪个商品；另一张是用户相似度图，如果两个用户的评论特征很相似，就给他们连一条线。然后在这两张图上跑 Louvain 社区检测发现水军团伙，用 PageRank 找出团伙里的核心人物，计算各种中心性指标。
>
> **第四步是集成学习。** 针对类别不平衡，我用了三层策略：数据层面用 SMOTETomek 组合采样（SMOTE 做少数类过采样，Tomek Links 清理边界噪声），模型层面设类别权重让模型更重视水军，评估层面做阈值优化找到最佳分类门槛。训练了 XGBoost、LightGBM、CatBoost、RandomForest 四个模型，做 Voting 和 Stacking 两种集成。
>
> **第五步是可解释性分析。** 用 SHAP 分析全局特征重要性，用 LIME 对每条预测给出局部解释——这条评论为什么被判定为水军，Top 3 原因是什么。
>
> **第六步是 BERT 语义增强，这是效果提升最大的环节。** 之前的特征都是"表层特征"——长度、标点数，水军换个写法就能绕过。我用 Sentence-BERT 提取评论的深层语义向量，384 维，和手工特征拼接。加入 BERT 之后，F1 从 0.58 直接跳到 0.84，提升了约 45%。
>
> **第七步是图特征融合，** 把图网络分析中算出的度、聚类系数、PageRank 等 5 个图特征也加进去。
>
> **第八步是最终报告，** 输出性能对比、特征重要性排序、可视化图表。
>
> 最终模型 F1 是 0.84，准确率 95.7%，精确率 86.6%，召回率 82.0%。
>
> 这个项目的核心能力其实和数据开发高度一致——ETL 流程设计、数据建模、性能优化、结果可解释。如果把这个项目的 pandas 实现换成 Spark/PySpark，就是一套分布式数据处理 Pipeline。

---

## ⭐ 1.4 项目介绍 —— 1分钟极速版（逐字背诵）

> 我做了个电商水军检测系统。12万条评论，水军占比不到10%，类别严重不平衡。
>
> 整个流程分三步：首先是特征工程，从文本、行为、时间、评分四个角度提取了 20 多个特征；然后是建模，针对不平衡问题用 SMOTE 采样 + 四个模型集成；最后用 BERT 提取语义特征，这是效果提升最大的部分，F1 从 0.58 提升到 0.84。
>
> 这个项目完整覆盖了 ETL、数据建模、性能优化的全流程，和数据开发工作高度一致。

---

## 1.5 项目延展：8 个步骤的深层解释（理解后复述）

### Step 1：特征工程 —— 为什么选这 20 个特征？

```
水军有四种异常模式，我的特征设计就是针对这四种模式的：
1. 文本模式异常 → 短文本、模板化、重复字符、标点夸张 → 文本统计特征
2. 行为模式异常 → 高频评论、评分极端、时间规律 → 用户行为特征
3. 时间模式异常 → 深夜批量作业 → 时间特征
4. 评分模式异常 → 全5星或全1星 → 评分特征

四个维度交叉验证：就算一个维度被水军规避了（比如换写法），其他维度还能抓到它。
```

### Step 2：无监督检测 —— 三种方法通俗理解

```
Isolation Forest：像在人群中找"很容易被孤立的那个人"
DBSCAN：基于密度的聚类，水军密度太低，进不了任何簇，被当成噪声
LOF：看一个人的邻居，如果他的行为和邻居都不一样，就很可疑

为什么要投票？单一方法可能误判，三个方法都说可疑，才真正可疑。
```

### Step 3：图网络分析 —— 核心价值

```
传统的特征工程看的是"一个人"可不可疑
图分析看的是"这人和谁一伙"
水军的核心特征恰好是"团伙作案"，所以图分析能发现特征工程漏掉的模式

用户-商品二部图 → 对应数仓中事实表 join 用户维表和商品维表
Louvain 社区检测 → 不需要提前知道有多少个团伙，算法自动发现
PageRank → Google 给网页排名的算法，用在团伙里找出"老大"
```

### Step 4：集成学习 —— 类别不平衡三层策略

```
为什么不能直接训练？因为水军才占 10%，模型只要全部猜"正常"就能蒙对 90%，但这没意义。

第一层 SMOTETomek：
  SMOTE = 在两个水军样本之间的连线上随机取点，生成"人造水军"
  Tomek Links = 删除分类边界上模棱两可的样本，让边界更清晰

第二层 类别权重：
  scale_pos_weight = 正常数量/水军数量 ≈ 10
  意思是"漏掉一个水军的代价 = 漏掉 10 个正常用户的代价"

第三层 阈值优化：
  默认预测概率 > 0.5 判为水军
  但在不平衡场景下，0.5 不一定是最好门槛
  我遍历了 0.3~0.7，找到 F1 最高的最优阈值，实际发现大约在 0.35~0.40
```

### Step 5：可解释性 —— SHAP 和 LIME 的区别

```
SHAP：全局视角。"在所有预测中，哪些特征总体最重要？"
LIME：局部视角。"针对这条具体的评论，为什么模型判它是水军？"

SHAP 有严格数学理论（博弈论 Shapley 值），LIME 是近似方法
项目中 SHAP 用来做报告中的特征重要性排序，LIME 用来解释单条预测
```

### Step 6：BERT —— 为什么能带来 45% 的提升？

```
传统特征看得是"表面"——评论多长、多少感叹号
BERT 看得是"含义"——评论是真心夸还是敷衍模板

打个比方：
  "太好了！！！强烈推荐！！！"  ← 传统特征直接抓到（感叹号多）
  "该产品使用体验超出预期，各方面表现令人满意"  ← 老特征抓不到，BERT 看得出来是模板

水军的核心特征是：不同账号用不同的词，但表达的意思完全相同（模板化语义）
BERT 能捕捉到这种"不同措辞但相同语义"的模式
```

### Step 7：图特征 —— F1 没涨为什么还要做？

```
图特征加入后 F1 从 0.8425 降到 0.8403，几乎持平。

但图特征的价值不在于涨指标：
1. SHAP 分析显示图特征贡献了约 8-10% 的重要性，模型确实在用
2. 有些水军写得很自然（BERT 难识别），但和同伙行为模式高度一致（图特征能识别）
3. 业务方最关心的是"有多少团伙、头目是谁"，图分析能直接回答这个问题
```

---

## ⭐ 1.6 项目核心数字（必须背熟，当场脱口而出）

| 数字 | 含义 |
|------|------|
| 12 万条 | 评论数据总规模 |
| 10:1 | 正常评论 vs 水军评论 |
| 20+ | 手工提取的特征数量 |
| 384 维 | BERT 语义向量维度 |
| 4 个模型 | XGBoost / LightGBM / CatBoost / RandomForest |
| 3 层策略 | SMOTE采样 + 类别权重 + 阈值优化 |
| 8 步 | 完整 Pipeline 步骤 |
| 0.53 | 基线 F1 分数 |
| 0.84 | 最终 F1 分数 |
| 95.7% | 最终准确率 |
| 86.6% | 最终精确率 |
| 82.0% | 最终召回率 |

---

## 1.7 项目难点与解决（展示解决问题的能力）

| 难点 | 我怎么解决的 | 体现什么能力 |
|------|------------|-------------|
| 类别严重不平衡 10:1 | SMOTETomek + class_weight + 阈值优化，三层组合拳 | 数据工程思维 |
| 标注数据可能不全 | 无监督检测做补充，投票机制 | 系统性思维 |
| 12万用户两两算相似度内存溢出 | 分批计算 + 限制边数上限 + 采样 5000 | 工程优化能力 |
| BERT 模型可能加载失败 | 设计 3 层 fallback：主模型→备选模型→TF-IDF | 鲁棒性设计 |
| 模型是黑盒不可解释 | SHAP 全局 + LIME 局部，双管齐下 | 业务落地能力 |
| 水军模式不断变化 | 多维特征交叉 + 多模型集成 + 图分析 | 全面性思维 |

---

## 1.8 gnucash-web 财务项目话术（备用）

> 这是一个基于 Vue3 + Element Plus 的财务 Web 端。我负责前端整体架构设计和核心模块开发，包括会计科目树、资产负债表、利润表、试算平衡表、凭证录入等。
>
> 和数据开发相关的是：会计系统本质上就是对数据的建模和聚合。科目是树形层级模型，报表是对底层事实数据的 SUM/GROUP BY 聚合，借贷平衡是数据校验规则。我在这个项目中深入理解了多表关联查询、数据流转、以及接口数据规范。
>
> 技术点：Vue3 Composition API、Element Plus、Axios 拦截器（注入当前账簿 ID 实现多账簿隔离）、RESTful API 对接。

---

## 1.9 农富坊大创项目话术（备用）

> 这是一个大学生创新创业项目，做的是连接红色文化资源与乡村旅游的数字化服务平台。
>
> 和数据开发的关系：这个项目培养了我的业务理解能力。比如文旅数据有明显的季节性波动和地域聚集性，这让我后续做特征工程时对时间特征和分组特征特别敏感。数据开发不只是写 SQL，更重要的是理解业务场景。

---

# 第二篇：SQL 全集 —— 面试必考核心

---

## ⭐ 2.1 SQL 执行顺序（必背）

```
书写顺序：SELECT → FROM → JOIN → WHERE → GROUP BY → HAVING → ORDER BY → LIMIT
执行顺序：FROM → JOIN → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT
```

**为什么重要？**
- `WHERE` 里不能用 `SELECT` 中定义的别名（因为 WHERE 先执行）
- `WHERE` 里不能用聚合函数（SUM/COUNT/AVG），`HAVING` 里可以（因为 HAVING 在 GROUP BY 之后）

---

## ⭐ 2.2 窗口函数（最重要，面试必问）

### 基本语法

```sql
函数名() OVER (
    [PARTITION BY 列1, 列2]    -- 分区，相当于 GROUP BY 但不压缩行
    [ORDER BY 列3]             -- 排序
    [ROWS/RANGE ...]           -- 窗口范围（可选）
)
```

### 三大类窗口函数（必背表格）

| 类别 | 函数名 | 作用 | 典型场景 |
|------|--------|------|---------|
| 排序类 | `ROW_NUMBER()` | 给行编号，1,2,3,4...不重复 | 去重、TopN |
| 排序类 | `RANK()` | 排名，1,1,3,4...重复就跳号 | 排名 |
| 排序类 | `DENSE_RANK()` | 排名，1,1,2,3...重复不跳号 | 排名（不跳号） |
| 聚合类 | `SUM() / AVG() / COUNT() / MAX() / MIN()` | 在窗口内聚合 | 累计、滑动 |
| 偏移类 | `LAG(列, n)` | 往前取第 n 行 | 环比 |
| 偏移类 | `LEAD(列, n)` | 往后取第 n 行 | 同比 |

### ROW_NUMBER vs RANK vs DENSE_RANK（必背）

```
假设分数：100, 100, 90

ROW_NUMBER(): 1, 2, 3  （不重复，去重用这个）
RANK():       1, 1, 3  （重复跳号）
DENSE_RANK(): 1, 1, 2  （重复不跳号）
```

---

## ⭐ 2.3 经典 SQL 题 10 道（每道都要能手写）

### 题1：每个用户最近一次订单金额（TopN）

```sql
SELECT user_id, order_amount
FROM (
    SELECT user_id, order_amount,
           ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY order_date DESC) AS rn
    FROM orders
) t
WHERE rn = 1;
```

### 题2：每个部门工资前 3 名的员工（分组 TopN）

```sql
SELECT dept_id, employee_name, salary
FROM (
    SELECT dept_id, employee_name, salary,
           DENSE_RANK() OVER (PARTITION BY dept_id ORDER BY salary DESC) AS rk
    FROM employee
) t
WHERE rk <= 3;
```

### 题3：连续 3 天登录的用户（连续问题）

```sql
-- 核心思路：日期 - 排名序号 = 差值。连续日期的差值相等
SELECT DISTINCT user_id
FROM (
    SELECT user_id, login_date,
           DATE_SUB(login_date, INTERVAL
               ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY login_date) DAY
           ) AS diff
    FROM (SELECT DISTINCT user_id, login_date FROM user_logins) t1
) t2
GROUP BY user_id, diff
HAVING COUNT(*) >= 3;
```

### 题4：每月销售额及累计到当月的总额（累计求和）

```sql
SELECT month, monthly_amount,
       SUM(monthly_amount) OVER (ORDER BY month) AS cum_amount
FROM monthly_sales;
```

### 题5：当月销售额与上个月的差值（环比）

```sql
SELECT month, amount,
       LAG(amount, 1) OVER (ORDER BY month) AS last_month_amount,
       amount - LAG(amount, 1) OVER (ORDER BY month) AS diff
FROM sales;
```

### 题6：每个用户第一笔订单和最后一笔订单的时间差

```sql
SELECT user_id,
       FIRST_VALUE(order_date) OVER (PARTITION BY user_id ORDER BY order_date) AS first_order,
       LAST_VALUE(order_date) OVER (PARTITION BY user_id ORDER BY order_date
           ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS last_order,
       DATEDIFF(
           LAST_VALUE(order_date) OVER (PARTITION BY user_id ORDER BY order_date
               ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING),
           FIRST_VALUE(order_date) OVER (PARTITION BY user_id ORDER BY order_date)
       ) AS days_span
FROM orders;
```

### 题7：留存率计算（次日留存）

```sql
SELECT first_day,
       COUNT(DISTINCT t1.user_id) AS new_users,
       COUNT(DISTINCT t2.user_id) / COUNT(DISTINCT t1.user_id) AS day1_retention
FROM (
    SELECT user_id, MIN(login_date) AS first_day
    FROM user_logins
    GROUP BY user_id
) t1
LEFT JOIN user_logins t2
    ON t1.user_id = t2.user_id AND t2.login_date = DATE_ADD(t1.first_day, INTERVAL 1 DAY)
GROUP BY first_day;
```

### 题8：行列转换 —— 行转列（每个用户各科成绩）

```sql
-- 原始表 scores(user_name, subject, score) → 目标表(user_name, 语文, 数学, 英语)
SELECT user_name,
       MAX(CASE WHEN subject = '语文' THEN score END) AS 语文,
       MAX(CASE WHEN subject = '数学' THEN score END) AS 数学,
       MAX(CASE WHEN subject = '英语' THEN score END) AS 英语
FROM scores
GROUP BY user_name;
```

### 题9：行列转换 —— 列转行（UNION ALL）

```sql
SELECT user_name, '语文' AS subject, 语文 AS score FROM wide_table
UNION ALL
SELECT user_name, '数学' AS subject, 数学 AS score FROM wide_table
UNION ALL
SELECT user_name, '英语' AS subject, 英语 AS score FROM wide_table;
```

### 题10：用户最大连续登录天数

```sql
-- 在题3的基础上取每个用户的最大连续天数
SELECT user_id, MAX(consecutive_days) AS max_consecutive
FROM (
    SELECT user_id, diff, COUNT(*) AS consecutive_days
    FROM (
        SELECT user_id, login_date,
               DATE_SUB(login_date, INTERVAL
                   ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY login_date) DAY
               ) AS diff
        FROM (SELECT DISTINCT user_id, login_date FROM user_logins) t1
    ) t2
    GROUP BY user_id, diff
) t3
GROUP BY user_id;
```

---

## ⭐ 2.4 JOIN 全解

### JOIN 类型（必背）

```sql
INNER JOIN     — 两表都有匹配才保留
LEFT JOIN      — 左表全保留，右表无匹配填 NULL（最常用）
RIGHT JOIN     — 右表全保留
FULL OUTER JOIN — 两表全保留（MySQL 不支持，用 UNION 模拟）
CROSS JOIN     — 笛卡尔积（每行 × 每行）
```

### LEFT JOIN 陷阱（高频面试题）

```sql
-- 写法 A：在 ON 中过滤 → 保留左表全部行，右表无匹配填 NULL
SELECT * FROM A LEFT JOIN B ON A.id = B.id AND B.status = 1;

-- 写法 B：在 WHERE 中过滤 → 把 NULL 行也过滤掉了，变成 INNER JOIN 的效果！
SELECT * FROM A LEFT JOIN B ON A.id = B.id WHERE B.status = 1;
```

> **记忆口诀**：ON 中过滤不影响左表行数，WHERE 中过滤会把 NULL 干掉。

---

## 2.5 SQL 优化基础

### 索引原理（理解即可）

```
索引 = 排序好的数据结构（B+ 树），查询时二分查找，O(logN)
没有索引 = 全表扫描，一行一行找，O(N)

聚簇索引：数据和索引在一起（InnoDB 的主键），只能有一个
非聚簇索引：索引存的是主键值，回表查到主键后再查数据

最左前缀原则：
  联合索引 (a, b, c) 相当于建了三个索引：
  (a) 可用、(a,b) 可用、(a,b,c) 可用
  (b) 不可用、(b,c) 不可用、(c) 不可用 ← 跳过第一列就失效
```

### SQL 优化 5 条军规

```
1. SELECT 只取需要的列，别用 SELECT *
2. WHERE 中别对索引列做函数运算
   ✗ WHERE DATE(create_time) = '2024-01-01'
   ✓ WHERE create_time >= '2024-01-01' AND create_time < '2024-01-02'
3. 大表分页用游标分页，别用 LIMIT 100000, 10
4. 小表驱动大表：子查询结果小用 IN，子查询结果大用 EXISTS
5. 能用 UNION ALL 别用 UNION（UNION 多了一步去重排序）
```

### EXPLAIN 看什么（知道概念即可）

```
type 列（从好到差）：system > const > eq_ref > ref > range > index > ALL
  ALL 是全表扫描，必须避免
  ref 是用到了索引，可以接受
  const 是用主键或唯一索引查单行，最优

key 列：实际使用的索引名
rows 列：预估扫描行数，越小越好
Extra 列：Using filesort / Using temporary 都是不好的信号
```

---

## 2.6 高频 SQL 概念速问速答

**Q: UNION 和 UNION ALL 区别？**
> UNION 去重（多一步排序操作），UNION ALL 不去重。能用 UNION ALL 就别用 UNION。

**Q: WHERE 和 HAVING 区别？**
> WHERE 在 GROUP BY 之前过滤行，不能用聚合函数；HAVING 在 GROUP BY 之后过滤分组，能用聚合函数。

**Q: DELETE / TRUNCATE / DROP 区别？**
> DELETE：删行，可回滚，可加 WHERE，不释放空间
> TRUNCATE：清空表，不可回滚，重置自增 ID，释放空间
> DROP：直接删表（连结构一起删）

**Q: CHAR 和 VARCHAR 区别？**
> CHAR：定长，存不满也占满空间（快但浪费）
> VARCHAR：变长，按实际长度 + 1-2 字节存长度（省空间）

**Q: COUNT(*) vs COUNT(1) vs COUNT(列名)？**
> COUNT(*) = COUNT(1)：计行数，一样快
> COUNT(列名)：不计 NULL 值，只计该列非空的行

---

# 第三篇：数据仓库与 ETL

---

## ⭐ 3.1 数据仓库 vs 数据库（必背对比表）

| 对比维度 | 数据库（OLTP） | 数据仓库（OLAP） |
|---------|--------------|----------------|
| 英文全称 | On-Line Transaction Processing | On-Line Analytical Processing |
| 用途 | 业务交易处理（下单、支付） | 分析决策支持（报表、BI） |
| 数据特点 | 当前最新、细节数据 | 历史累积、汇总数据 |
| 读写模式 | 频繁增删改查 | 批量写入、大量查询 |
| 数据量级 | GB 级 | TB ~ PB 级 |
| 设计范式 | 3NF（减少冗余） | 维度建模（适当冗余换速度） |
| 典型产品 | MySQL, Oracle, PostgreSQL | Hive, ClickHouse, Doris |
| 服务级别 | 高并发、低延迟 (ms) | 高吞吐、可接受延迟 (s~min) |

---

## ⭐ 3.2 数仓四层分层（必背）

```
┌──────────────────────────────────────────┐
│  ADS  (Application Data Service)         │  应用数据层
│  面向具体应用：报表、BI看板、算法特征表     │  "直接拿来用"
├──────────────────────────────────────────┤
│  DWS  (Data Warehouse Summary)           │  汇总数据层
│  按主题轻度聚合：用户日活、商品日销量       │  "轻度聚合"
├──────────────────────────────────────────┤
│  DWD  (Data Warehouse Detail)            │  明细数据层
│  清洗后的业务事实明细 + 维度退化           │  "干净明细"
├──────────────────────────────────────────┤
│  ODS  (Operational Data Store)           │  贴源数据层
│  原始数据原封不动同步，结构和源系统一致      │  "原样备份"
└──────────────────────────────────────────┘
```

> **记忆口诀**：O-D-D-A（欧弟弟诶），贴源→明细→汇总→应用

**各层做什么（一句话版）：**
- **ODS**：原封不动从业务库同步过来，保留原始面貌，做备份和溯源
- **DWD**：对 ODS 做清洗（去重、空值处理、格式统一），形成干净的明细事实表
- **DWS**：按主题（用户/商品/订单）做轻度聚合，形成主题宽表
- **ADS**：面向具体应用场景，BI 直接取数，算法直接读取

---

## ⭐ 3.3 ETL 详解

### ETL 三个字母

```
E = Extract  抽取：从源系统（MySQL/API/文件）获取数据
T = Transform 转换：清洗、格式转换、业务计算、关联整合
L = Load     加载：写入目标系统（数仓/数据湖）
```

### 结合我的项目解释 ETL（面试话术）

> 在网络水军项目中，虽然没用传统的 ETL 工具（如 Kettle），但我实现了一套完整的 ETL 流程：
> - **Extract**：从 Excel 原始数据读取 12 万条评论记录
> - **Transform**：jieba 分词、停用词过滤、特征计算（文本统计、行为聚合、时间维度转换）、标准化
> - **Load**：输出为 features.csv 特征宽表，供下游模型使用
>
> ETL 的核心不是工具，而是思维——数据质量保障、转换规则可复用、上下游依赖管理。

### 增量抽取三种方式

| 方式 | 怎么做 | 优点 | 缺点 |
|------|-------|------|------|
| 时间戳增量 | WHERE update_time > '上次同步时间' | 简单 | 删除了的数据感知不到 |
| 自增 ID 增量 | WHERE id > 上次最大 id | 简单 | 更新和删除感知不到 |
| CDC（变更数据捕获） | 监听 MySQL binlog，捕获所有 INSERT/UPDATE/DELETE | 实时、完整 | 复杂，需要 Canal/Debezium |

### 全量 vs 增量

```
全量抽取：每次都把整个表拉一遍 → 简单但慢，适合小数据量
增量抽取：只拉变化的数据 → 快但需要维护状态，适合大数据量
T+1 模式：今天处理昨天的数据，批处理，离线的标准做法
```

---

## ⭐ 3.4 数据质量六大维度（必背）

| 维度 | 含义 | 检查什么 |
|------|------|---------|
| **完整性** | 该有的都有吗？ | 空值率、必填字段缺失 |
| **准确性** | 数据对吗？ | 数值范围、枚举值是否合法 |
| **一致性** | 不同地方一样吗？ | 同一数据在不同系统中是否一致 |
| **及时性** | 按时到了吗？ | 数据是否在约定时间到达 |
| **唯一性** | 有重复吗？ | 主键唯一、无重复记录 |
| **有效性** | 格式对吗？ | 手机号格式、邮箱格式、日期格式 |

---

## 3.5 Lambda 架构 vs Kappa 架构（了解即可，展示视野）

```
Lambda 架构：离线层（批处理） + 实时层（流处理） + 服务层
  优点：成熟稳定，离线修正实时结果
  缺点：维护两套代码，逻辑要写两遍

Kappa 架构：全部用流处理，离线数据回放流处理即可
  优点：一套代码
  缺点：对消息队列要求高，需要存全部历史消息
```

---

# 第四篇：数据建模

---

## ⭐ 4.1 维度建模核心概念（必背）

### 事实表 vs 维度表

```
事实表 (Fact Table)：
  - 存的是业务度量值，可累加的数值
  - 如：订单金额、数量、折扣
  - 特点：行多、列少、增长快
  - 粒度：一行代表一次业务事件

维度表 (Dimension Table)：
  - 存的是业务实体的描述信息
  - 如：用户信息（姓名、年龄、地区）、商品信息（名称、类目、价格带）
  - 特点：行少、列多、变化慢
```

### 星型模型 vs 雪花模型（必背）

```
星型模型：
  事实表在中间，维度表直接关联
  维度表有冗余（如地区表里省/市/区都冗余在一行）
  关联少、查询快、OLAP 首选 ← 实际工作中大多数情况用这个

雪花模型：
  维度表进一步规范化，拆成多层级（地区表 → 省表 + 市表 + 区表）
  冗余少，但查询要多层 JOIN，慢
  理论上更规范，实际少用
```

> **记忆**：星型=冗余但快，雪花=规范但慢。数仓追求查询速度 → 星型为主。

---

## ⭐ 4.2 缓慢变化维 SCD（面试高频）

> 维度表的数据会变（如用户改了地址），历史数据怎么办？

| 类型 | 做法 | 效果 | 适用场景 |
|------|------|------|---------|
| **Type 1** | 旧值直接覆盖 | 丢了历史 | 不关心历史，只要最新值 |
| **Type 2** | 新增一行 + 生效/失效时间戳 | 保留完整历史 | **最常用**，如用户地址变更历史 |
| **Type 3** | 加一个"旧值"列 | 只保留上一版 | 有时需要和上一期对比 |

> **面试话说**："实际生产环境中 Type 2 最常用，因为它保留了完整的历史变更轨迹，支持任意时间点的回溯分析。"

---

## 4.3 数据建模三步法（维度建模流程）

```
第一步：选定业务过程
  明确要分析什么业务（如"电商订单分析"）

第二步：声明粒度
  事实表中一行代表什么？（如"一行 = 一个订单的一个商品"）
  粒度是最关键的决策！粒度定了就不能随便改

第三步：确定维度
  从哪些角度看？（用户、商品、时间、地区...）

第四步：确定事实（度量）
  看什么数字？（订单金额、数量、折扣金额...）
```

---

## 4.4 结合我的项目谈建模（面试话术）

> 网络水军项目中的图建模可以转化为数仓语言：
> - **事实表（fact_comment）**：用户ID、商品ID、评论内容、评分、评论时间
> - **维度表 dim_user**：用户ID、注册时间、等级
> - **维度表 dim_product**：商品ID、类目、价格带
>
> 二部图的分析本质就是：事实表 JOIN 两个维度表后，在图层面（而不是 SQL GROUP BY 层面）做关联发现。图算法能发现比 SQL 聚合更复杂的隐式关系——比如两个用户从没互动过，但评论模式高度相似，在图上就能被 Louvain 聚到同一个社区。

---

# 第五篇：大数据组件全套

---

## ⭐ 5.1 Kafka（消息队列）

### 一句话定位
> Kafka 是**高吞吐的分布式消息队列**，在实时数仓中担任**数据采集层**，把业务系统的日志/事件实时传输到下游。

### 核心概念（必背 7 个词）

| 概念 | 解释 | 通俗类比 |
|------|------|---------|
| **Producer** | 生产者，发送消息的 | 快递员 |
| **Consumer** | 消费者，读取消息的 | 收件人 |
| **Topic** | 消息主题，逻辑分类 | 快递柜的格子（按小区分） |
| **Partition** | 主题的分区，物理分片 | 格子里的多个隔间 |
| **Offset** | 消息在分区中的序号 | 隔间里的编号 |
| **Broker** | Kafka 服务器节点 | 快递柜本身 |
| **Consumer Group** | 消费者组，组内分摊消费 | 多个快递员分片区配送 |

### Kafka 四大优势（为什么用 Kafka 不直接写数据库？）

```
1. 解耦：生产者和消费者互不依赖，各自独立演化
2. 削峰：流量高峰时消息暂存 Kafka，消费者按自己节奏慢慢消化
3. 高吞吐：单 Broker 可达数十万 QPS，支持 TB 级数据
4. 可回溯：通过 Offset 可以重新消费历史数据（像视频倒退重看）
```

### 消息不丢失的三板斧（必背）

```
Producer 端：acks=all（等所有副本确认才认为发送成功）
Broker 端：replication.factor >= 3 + min.insync.replicas >= 2（多副本）
Consumer 端：手动提交 Offset，处理完业务逻辑再 commit
```

### 消息顺序保证

```
同一 Partition 内：严格有序
不同 Partition 间：无序

需要全局有序 → 把相关消息发到同一个 Partition（按 Key 做 Hash）
如：同一个用户的所有事件都发到 Partition X，该用户的时序就是有序的
```

### Kafka 常见面试追问

**Q: Kafka 为什么快？**
> 1. 顺序写磁盘（比随机写快 100 倍）
> 2. 零拷贝技术（sendfile 系统调用，数据不经过用户态）
> 3. Page Cache（利用操作系统的页缓存）
> 4. 批量发送和压缩

**Q: 消费者组是什么？**
> 同一个 Consumer Group 内的消费者分摊消费不同的 Partition。
> 一个 Partition 只能被组内一个消费者消费（避免重复）。
> 不同的 Consumer Group 之间独立消费（实现广播效果）。

**Q: Kafka 怎么保证高可用？**
> 通过 ISR（In-Sync Replicas）机制：每个 Partition 有一个 Leader 和多个 Follower。Leader 处理读写，Follower 同步数据。Leader 挂了，Controller 从 ISR 中选一个新 Leader。

---

## ⭐ 5.2 Flink（流处理引擎）

### 一句话定位
> Flink 是**真正的流处理框架**（原生流处理），毫秒级延迟，精确一次语义。Spark Streaming 是微批处理，秒级延迟。

### Flink vs Spark Streaming（必背对比表）

| 对比维度 | Flink | Spark Streaming |
|---------|-------|-----------------|
| 处理模式 | 原生流处理（一条一条处理） | 微批处理（攒几秒再处理） |
| 延迟 | 毫秒级 | 秒级 |
| 语义保证 | Exactly-Once（精确一次） | Exactly-Once（实现较复杂） |
| 状态管理 | 内置 State Backend（内存/RocksDB） | 依赖外部存储 |
| 容错 | Checkpoint 快照机制 | RDD 血缘 + Checkpoint |
| 适用场景 | 实时风控、实时推荐、实时监控 | 准实时统计、定时报表 |

### Flink 核心五概念（必背）

```
1. Event Time（事件时间）
   事件真实发生的时间（推荐使用，能处理乱序数据）
   vs Processing Time（处理时间，最简单但不准确）

2. Watermark（水位线）
   衡量 Event Time 进度的机制
   含义："这个时间点之前的数据都到了，可以计算了"
   允许一定时间的乱序数据延迟到达
   如：Watermark = 最大 EventTime - 允许延迟 5 秒

3. Window（窗口）
   对无界数据流进行切分：
   - 滚动窗口 (Tumbling)：固定大小，不重叠。如每 5 分钟统计一次
   - 滑动窗口 (Sliding)：固定大小，可重叠。如每 1 分钟统计过去 5 分钟
   - 会话窗口 (Session)：按活动间隙划分。如用户 30 分钟没动作就断开会话

4. Checkpoint（检查点）
   定期保存状态快照（自动触发）
   故障恢复时从最近成功的 Checkpoint 恢复
   保证 Exactly-Once 的关键

5. Savepoint（保存点）
   手动触发（用于升级、迁移、调试）
   和 Checkpoint 格式相同但用途不同
```

### Flink 在实时数仓中的位置

```
业务系统 → Kafka → Flink（实时清洗 + 窗口聚合）→ ClickHouse/Doris/HBase → BI 看板
                ↓
            Flink（异常检测）→ 告警系统
```

### Flink 面试追问

**Q: 什么是反压（Backpressure）？**
> 下游处理速度跟不上上游发送速度，造成数据堆积。Flink 有天然的流控机制，反压会从下游逐级向上游传导，最终让 Source 端降低读取速度。

**Q: Checkpoint 的原理？**
> Flink 在所有算子的数据流中插入 Barrier（栅栏），Barrier 流过算子时触发状态快照。当所有算子的快照都完成后，Checkpoint 完成。基于 Chandy-Lamport 分布式快照算法。

---

## ⭐ 5.3 Hive（数据仓库工具）

### 一句话定位
> Hive 不是数据库，它是一个**翻译器**——把 HiveQL（类 SQL）翻译成 MapReduce/Tez/Spark 任务在 Hadoop 上执行。底层存储是 HDFS，计算引擎是 MapReduce/Tez/Spark。

### 核心概念

| 概念 | 解释 | 类比 |
|------|------|------|
| **分区 (Partition)** | 按某列的值分目录存储 | 按日期分文件夹 dt=2024-01-01/ |
| **分桶 (Bucket)** | 按 Hash 分到固定数量的文件 | 按 user_id Hash 取模分文件 |
| **ORC** | 列式存储格式，压缩+索引 | 比普通文本快 10-100 倍 |
| **Parquet** | 另一种列式存储格式，兼容性好 | Spark 生态首选 |

### 分区 vs 分桶

```
分区：
  - 目录级别切分
  - 适合离散值、常用于日期（dt=）
  - WHERE dt='2024-01-01' → 只扫描一个目录 → 快

分桶：
  - 文件级别切分
  - 适合连续值或经常 JOIN 的 Key（如 user_id）
  - 两个表按同一 Key 分同样数量的桶 → JOIN 时每个桶各自 JOIN → 快
```

### Hive 优化要点

```
1. 分区裁剪：WHERE 必须带分区字段，否则全表扫描
2. ORC + Snappy 压缩：列存 + 压缩 = 查询快 + 存储省
3. 避免小文件：小文件多 → Map Task 多 → 慢 → 用合并任务
4. MapJoin：把小表加载到内存广播，避免 Shuffle
   SET hive.auto.convert.join=true;
5. 避免数据倾斜：打散热点 Key + 两阶段聚合
6. 尽量用 Tez 或 Spark 引擎代替 MapReduce
```

### Hive 面试追问

**Q: Hive 和 MySQL 有什么区别？**
> MySQL 是 OLTP 数据库，支持实时增删改查，延迟毫秒级，数据量 GB 级。
> Hive 是 OLAP 数据仓库工具，只做批量分析查询，延迟分钟级，数据量 TB~PB 级。
> Hive 不支持行级 UPDATE/DELETE（高版本有限支持），数据存在 HDFS 上。

**Q: 内部表和外部表区别？**
> 内部表（Managed Table）：Hive 管理数据生命周期，DROP 表时数据也被删除。
> 外部表（External Table）：Hive 只管理元数据，DROP 表时数据还在 HDFS 上。
> 生产环境一般用外部表，数据安全。

---

## ⭐ 5.4 Hadoop 三件套（基础了解）

### HDFS（分布式文件系统）

```
NameNode：管理元数据（文件在哪里、分了几块），相当于"目录"
DataNode：存实际数据，相当于"书架"
SecondaryNameNode：不是热备！是定期合并 NameNode 的编辑日志

文件分块（Block，默认 128MB）+ 多副本（默认 3 份）
你存一个 1GB 的文件 → 切成 8 块 × 3 副本 = 24 块分散在集群中
```

### MapReduce（分布式计算框架）

```
Map 阶段：分而治之。每个数据块独立处理 → 输出 (key, value) 对
Shuffle 阶段：按 key 重新分发数据
Reduce 阶段：按 key 聚合

经典例子 WordCount：
  Map: "hello world hello" → (hello,1)(world,1)(hello,1)
  Shuffle: 把相同 key 发到一起
  Reduce: hello → 1+1=2, world → 1
```

### YARN（资源调度）

```
ResourceManager：整个集群的资源调度老大
NodeManager：每台机器上的"管家"，领任务、汇报资源
Container：实际运行任务的容器（分配 CPU + 内存）

Hive / Spark / Flink 都可以跑在 YARN 上
```

---

## 5.5 Spark（快速了解，别和 Flink 混淆）

### Spark 是什么

```
定位：分布式计算引擎，比 MapReduce 快 10-100 倍
快的核心原因：内存计算（中间结果放内存，不放磁盘）

Spark SQL：用 SQL 分析数据（对应 Hive，但快很多）
Spark Streaming：微批处理（对应 Flink Streaming）
Spark MLlib：机器学习库
GraphX：图计算库

和 Hive 的关系：Spark 可以作为 Hive 的计算引擎替代 MapReduce
```

### Spark vs MapReduce 为什么快？

```
1. 内存计算：中间结果放内存，MapReduce 每次都要落盘
2. DAG 调度：一次性规划整个计算图，MapReduce 是串行的
3. 线程级并行：Spark 用线程，MapReduce 用进程（线程更轻量）
```

---

## 5.6 其他常用组件（知道名字和用途即可）

| 组件 | 一句话定位 |
|------|----------|
| **Zookeeper** | 分布式协调服务。Kafka 用它做选举，HBase 用它存元数据 |
| **HBase** | 基于 HDFS 的 NoSQL 数据库，列式存储，适合实时查询海量数据 |
| **ClickHouse** | OLAP 列式数据库，查询极快，适合报表和 BI |
| **Doris** | 百度开源的 MPP 分析型数据库，支持高并发查询 |
| **Redis** | 内存缓存，常用于实时计算的中间结果缓存 |
| **Airflow** | 任务调度框架，管理 ETL 任务间的依赖和定时执行 |
| **Canal** | 阿里开源的 MySQL binlog 订阅工具，用于 CDC 数据同步 |
| **Flume** | 日志采集工具，把服务器日志收集到 Kafka/HDFS |

---

## 5.7 大数据组件协作全景图（心中要有这张图）

```
业务层          用户下单、浏览、支付...
                 ↓
数据采集层      Flume（日志采集）/ Canal（数据库变更）/ 业务直接写
                 ↓
消息缓冲层      Kafka（削峰 + 解耦 + 可回溯）
                 ↓
实时计算层      Flink（实时清洗、窗口聚合、异常检测）
                 ↓                    ↓
实时存储层      HBase/Redis      离线存储层 HDFS → Hive/Spark
                 ↓                    ↓
OLAP 引擎层    ClickHouse/Doris   数仓分层 ODS-DWD-DWS-ADS
                 ↓                    ↓
应用层         实时看板/告警        BI报表/数据分析/机器学习
```

---

# 第六篇：Java 基础速成

> 目标岗位要求 Java，我的策略：会基础语法 + 能看懂接口代码 + 坦诚水平 + 强调学习能力

---

## 6.1 Java 基础概念速记

### 面向对象三大特性

```
封装：把数据和操作数据的方法包在一起，隐藏内部细节
继承：子类继承父类的属性和方法，实现代码复用
多态：同一个方法，不同对象调用表现出不同行为
```

### 基本数据类型（8 种）

```
整数：byte(1字节) / short(2) / int(4) / long(8)
浮点：float(4) / double(8)
字符：char(2)
布尔：boolean(1位)
```

### 集合框架（知道常用的）

```
List：有序、可重复
  - ArrayList：底层数组，查快改慢（扩容复制）
  - LinkedList：底层链表，增删快

Set：无序、不可重复
  - HashSet：基于 HashMap 实现

Map：键值对
  - HashMap：最常用，线程不安全
  - ConcurrentHashMap：线程安全版本
```

### HashMap 原理（高频，理解即可）

```
底层：数组 + 链表 + 红黑树（JDK 8+）
存入时：
  1. 计算 Key 的 hashCode
  2. hashCode 取模得到数组下标
  3. 该位置为空 → 直接放
  4. 该位置有值（Hash 冲突）→ 链表追加 / 红黑树
  5. 链表长度 > 8 且数组长度 >= 64 → 转红黑树

扩容：默认容量 16，负载因子 0.75，超过 75% 就扩容为 2 倍
```

### == 和 equals 的区别

```
== 比较的是栈中的值：
  基本类型：比较值是否相等
  引用类型：比较内存地址是否相同

equals 默认比较的是地址，但 String 等类重写了它，比较内容
→ 比较字符串内容用 equals，别用 ==
```

### String、StringBuilder、StringBuffer

```
String：不可变，每次修改都创建新对象（占用内存）
StringBuilder：可变，线程不安全，单线程用（快）
StringBuffer：可变，线程安全（synchronized），多线程用（慢一点）
```

---

## 6.2 Java 面试追问应对

**Q: JDK、JRE、JVM 是什么关系？**
> JDK 包含 JRE，JRE 包含 JVM。JDK 是开发工具包（编译器+运行时），JRE 是运行环境（JVM+核心类库），JVM 是虚拟机（执行字节码）。

**Q: 重载 (Overload) 和重写 (Override) 区别？**
> 重载：同一个类中，方法名相同，参数不同（个数/类型/顺序）
> 重写：子类重写父类方法，方法签名完全一致，返回值类型兼容

**Q: 接口和抽象类的区别？**
> 接口：只能有抽象方法和常量（JDK 8 后可以有默认方法），一个类可以同时实现多个接口
> 抽象类：可以有具体方法和成员变量，一个类只能继承一个抽象类

**Q: 异常处理？**
> try-catch-finally：finally 里的代码一定会执行（除非 System.exit）
> throws：把异常往上抛，谁调用谁处理
> Checked Exception：编译时强制处理的异常（如 IOException）
> Unchecked Exception：运行时异常（如 NullPointerException），不强制处理

**Q: 你对 Java 的掌握程度？**
> 坦诚回答："我的主力语言是 Python，Java 不是最擅长的。但我能看懂 Java 后端的 RESTful 接口代码，理解返回的 JSON 数据结构。在 gnucash-web 项目中，我和 Java 后端同事协作开发。而且 Flink 的 DataStream API 支持 Java，我了解基本语法。我的学习能力比较强——项目中我从零学会 BERT 和 GNN，如果有需要我可以快速上手 Java。"

---

# 第七篇：Python 基础（因为你的项目是 Python 写的）

---

## 7.1 高频 Python 概念

### 可变对象 vs 不可变对象

```
不可变：int, float, str, tuple → 修改时会创建新对象
可变：list, dict, set → 原地修改

→ 函数传参时，不可变对象相当于"传值"，可变对象相当于"传引用"
```

### 列表推导式

```python
# 传统写法
result = []
for i in range(10):
    if i % 2 == 0:
        result.append(i ** 2)

# 推导式
result = [i ** 2 for i in range(10) if i % 2 == 0]
```

### 装饰器（知道概念即可）

```
本质：一个函数，接收函数作为参数，返回一个新函数
作用：在不修改原函数代码的前提下，给函数增加额外功能
常见用途：日志记录、性能计时、权限校验、缓存
```

### 生成器（Generator）

```
用 yield 代替 return，函数变成生成器
不一次性生成全部结果，而是一个一个地"吐出"
好处：省内存。处理超大文件时不会把整个文件加载到内存
```

### GIL（全局解释器锁）

```
GIL 是 CPython 的限制：同一时刻只有一个线程执行 Python 字节码
→ Python 多线程在 CPU 密集型任务中无法利用多核
→ IO 密集型任务（网络请求、文件读写）多线程仍然有用
→ CPU 密集型用多进程（multiprocessing）
```

### Pandas 常用操作速记

```python
df = pd.read_excel('file.xlsx')         # 读取
df = pd.read_csv('file.csv')
df.head()                                # 看前几行
df.info()                                # 看数据类型和空值
df.describe()                            # 统计摘要
df.isnull().sum()                        # 检查空值
df.dropna()                              # 删除空值
df.fillna(0)                             # 填充空值
df['new_col'] = df['a'] + df['b']       # 创建新列
df.groupby('user_id')['score'].mean()    # 分组聚合
df.merge(other_df, on='key')             # 表关联
df.to_csv('output.csv', index=False)     # 保存
```

---

# 第八篇：计算机网络基础

---

## ⭐ 8.1 高频网络概念

### OSI 七层模型 vs TCP/IP 四层

```
OSI 七层（理论模型）：          TCP/IP 四层（实际使用）：
7. 应用层                       应用层 (HTTP, DNS, FTP)
6. 表示层                       ↓
5. 会话层                       传输层 (TCP, UDP)
4. 传输层                       ↓
3. 网络层                       网络层 (IP)
2. 数据链路层                   网络接口层
1. 物理层
```

### TCP vs UDP（必背）

| 对比 | TCP | UDP |
|------|-----|-----|
| 连接 | 面向连接（三次握手） | 无连接 |
| 可靠性 | 可靠，保证不丢不乱 | 不可靠，丢了就丢了 |
| 速度 | 慢 | 快 |
| 顺序 | 有序 | 无序 |
| 场景 | HTTP、文件传输、邮件 | 视频直播、语音通话、DNS |

### TCP 三次握手（背下来）

```
第一次：客户端 → 服务器  SYN（我要连接，说句话）
第二次：服务器 → 客户端  SYN+ACK（好的，你也回我一句）
第三次：客户端 → 服务器  ACK（我收到了，开始传数据吧）

为什么是三次？防止失效的连接请求到达服务器导致错误建立连接。
```

### TCP 四次挥手（背下来）

```
第一次：客户端 → 服务器  FIN（我说完了）
第二次：服务器 → 客户端  ACK（我知道了，等我收完最后的）
第三次：服务器 → 客户端  FIN（我也说完了）
第四次：客户端 → 服务器  ACK（好的，拜拜）

为什么是四次？因为 TCP 是全双工的，两个方向要分别关闭。
```

### HTTP 请求方法

```
GET：获取资源（只读，参数在 URL 里，有长度限制）
POST：创建资源（参数在 Body 里，无长度限制）
PUT：全量更新资源
PATCH：部分更新资源
DELETE：删除资源
```

### HTTP 状态码

```
2xx 成功：200 OK / 201 Created
3xx 重定向：301 永久 / 302 临时
4xx 客户端错误：400 参数错误 / 401 未认证 / 403 无权限 / 404 不存在
5xx 服务端错误：500 内部错误 / 502 网关错误 / 503 服务不可用
```

### HTTP 和 HTTPS 的区别

```
HTTP：明文传输，80 端口
HTTPS：SSL/TLS 加密传输，443 端口
HTTPS 多了 TLS 握手（证书验证 + 密钥协商）
```

### Cookie / Session / Token

```
Cookie：浏览器端存储的小数据（key=value），自动附带在请求中
Session：服务器端存储的用户状态数据，通过 Cookie 中的 Session ID 关联
Token：加密签名的令牌（如 JWT），无状态，服务器不用存储
```

### DNS 解析过程（知道流程）

```
输入 www.baidu.com →
1. 浏览器缓存（自己有记录吗？）
2. 系统 hosts 文件（你手动配过吗？）
3. 本地 DNS 服务器（问最近的 DNS 服务器）
4. 根域名服务器（问 .com 在哪？）
5. 顶级域名服务器（问 baidu.com 在哪？）
6. 权威域名服务器（百度自己管的 DNS）
7. 返回 IP 地址
```

---

# 第九篇：操作系统基础

---

## 9.1 高频操作系统概念

### 进程 vs 线程

| 对比 | 进程 | 线程 |
|------|------|------|
| 定义 | 运行中的程序，资源分配的基本单位 | 进程内的执行流，CPU 调度的基本单位 |
| 资源 | 独立的内存空间、文件描述符 | 共享进程的内存和资源 |
| 通信 | IPC（管道、消息队列、共享内存） | 直接读写共享变量 |
| 开销 | 创建销毁开销大 | 创建销毁开销小 |
| 崩溃 | 一个进程崩溃不影响其他进程 | 一个线程崩溃可能拖垮整个进程 |

### 死锁（必背）

> 死锁：两个或多个进程互相等待对方释放资源，导致谁都动不了。

**四个必要条件（缺一不可）：**
```
1. 互斥：资源一次只能被一个进程使用
2. 持有并等待：持有一个资源，又在等别的资源
3. 不可剥夺：自己不放，别人抢不走
4. 循环等待：A 等 B → B 等 C → C 等 A

打破任意一个条件 = 解除死锁
```

### 并发 vs 并行

```
并发：一个 CPU 快速在多个任务间切换，看起来像同时执行（单核算法）
并行：多个 CPU 真正同时执行多个任务（多核硬件）
```

### 用户态 vs 内核态

```
用户态：运行普通应用程序，权限低，不能直接访问硬件
内核态：运行操作系统内核，权限高，可以访问所有资源
系统调用（如 read/write/网络 IO）会从用户态切换到内核态
```

### 内存管理

```
虚拟内存：每个进程看到的是"假地址"，由 MMU 映射到真实物理地址
好处：进程隔离、比物理内存更大的地址空间（部分数据可暂存磁盘）
```

---

# 第十篇：数据结构基础

---

## 10.1 高频数据结构速记

| 数据结构 | 特点 | 常见操作的时间复杂度 |
|---------|------|-------------------|
| **数组** | 连续内存，随机访问 O(1) | 查找 O(1)，插入删除 O(n) |
| **链表** | 分散内存，每个节点指向下一个 | 查找 O(n)，插入删除 O(1) |
| **栈** | 后进先出（LIFO） | push/pop O(1) |
| **队列** | 先进先出（FIFO） | enqueue/dequeue O(1) |
| **哈希表** | Key-Value，通过哈希函数定位 | 平均 O(1)，最坏 O(n) |
| **二叉树** | 每个节点最多两个孩子 | 二叉搜索树：查找/插入/删除 平均 O(logN) |
| **堆** | 完全二叉树，父节点 ≥ 子节点（大顶堆） | 插入 O(logN)，取最大 O(1) |

### 数组 vs 链表

```
数组：
  优点：随机访问快（知道下标直接取）
  缺点：插入删除慢（要移动后面所有元素），大小固定

链表：
  优点：插入删除快（改指针就行），大小灵活
  缺点：查找慢（要从头一个个找），额外存指针占内存
```

### 栈和队列的应用

```
栈的应用：函数调用栈、括号匹配、撤销操作（Ctrl+Z）、浏览器的后退
队列的应用：BFS 广度优先搜索、消息队列、打印机任务队列
```

### 二叉树遍历

```
前序遍历：根 → 左 → 右
中序遍历：左 → 根 → 右（二叉搜索树中序 = 从小到大输出）
后序遍历：左 → 右 → 根
层序遍历：一层一层往下（BFS，用队列）
```

---

# 第十一篇：机器学习 & AI 基础

---

## 11.1 核心概念（因为你的项目是 ML 项目）

### 有监督 vs 无监督

```
有监督学习：训练数据有标签（知道正确答案），学 X→Y 的映射
  如：分类（这是不是水军？），回归（房价预测）
  
无监督学习：训练数据没有标签，让模型自己找结构
  如：聚类（把相似用户分堆），降维（PCA）
  我的项目中：Isolation Forest、DBSCAN、LOF 是无监督异常检测
```

### 训练集 / 验证集 / 测试集

```
训练集：用来训练模型（调参数，模型从中学规律）→ 约 60-80%
验证集：用来调超参数、选模型 → 约 10-20%
测试集：最终评估，绝对不能碰 → 约 10-20%

防止数据泄露：测试集必须假装"不存在"，直到最后一步才用
```

### 过拟合 vs 欠拟合

```
过拟合：模型太复杂，把训练集里的噪声也学了 → 训练集分数高，测试集分数低
  解决：加正则化、减少特征、增加数据量、Dropout、早停

欠拟合：模型太简单，连基本规律都没学到 → 训练集和测试集分数都很低
  解决：加特征、换更复杂模型、减少正则化
```

### 偏差 vs 方差

```
偏差：模型预测值和真实值的偏离程度 → 欠拟合的表现
方差：模型在不同训练集上预测结果的变化程度 → 过拟合的表现

Bagging（RandomForest）：降低方差
Boosting（XGBoost）：降低偏差
```

### 分类问题的评估指标（必背）

```
准确率 (Accuracy) = 猜对的 / 全部
  缺点：类别不平衡时无意义（全猜正常也能 90%）

精确率 (Precision) = 猜是水军且真的是水军的 / 所有猜是水军的
  含义：抓的人里面，多少抓对了？"宁缺毋滥"
  场景：不能冤枉好人（如反垃圾邮件，别把重要邮件拦住）

召回率 (Recall) = 猜是水军且真的是水军的 / 所有真正是水军的
  含义：所有坏人里面，抓到了多少？"宁可错杀不可放过"
  场景：不能漏掉坏人（如疾病筛查，得病的一定要查出来）

F1 分数 = 2 × Precision × Recall / (Precision + Recall)
  精确率和召回率的调和平均，两者都考虑

AUC：ROC 曲线下的面积
  衡量模型对正负样本的排序能力
  0.5 = 随机猜，1.0 = 完美排序
  对类别不平衡不敏感
```

### 交叉验证

```
K 折交叉验证：把训练集平均分成 K 份
  每次用 K-1 份训练 + 1 份验证，轮流 K 次
  取 K 次结果的平均值作为最终评估
  优点：充分利用数据，评估更稳定
```

---

## 11.2 面试常见 ML 知识追问

**Q: 为什么选择 F1 而不是准确率？**
> 因为类别严重不平衡（水军只占 10%）。如果全猜"正常"，准确率也能到 90%，但水军一个都没抓到，没意义。F1 同时考虑精确率和召回率，能更真实地反映在不平衡场景下的模型表现。

**Q: 精确率和召回率怎么权衡？**
> 调整分类阈值。降低阈值 → 更多预测为水军 → 召回率上升，精确率下降（宁可错杀）。升高阈值 → 更少预测为水军 → 精确率提升，召回率下降（宁缺毋滥）。项目中通过遍历阈值找 F1 最高的点。

**Q: 数据量不够怎么办？**
> 1. 数据增强（文本：同义词替换、回译；图像：旋转、翻转）
> 2. 迁移学习（用预训练模型，如 BERT，在大数据上预训练过了）
> 3. SMOTE 等过采样方法
> 4. 交叉验证（充分利用有限数据）

**Q: 什么是特征工程？为什么重要？**
> 特征工程是把原始数据转化为模型能用的特征的过程。数据决定了模型效果的上限，模型只是逼近这个上限。好的特征比复杂的模型更重要。我的项目证明了：BERT 特征是效果提升最大的部分。

---

# 第十二篇：场景设计题

---

## 12.1 常见场景题及答题框架

### 题1：设计一个数据仓库，分析电商平台的用户行为

**答题框架：**
```
1. 确定业务需求：运营想看什么？日活、留存、转化率、GMV

2. 数据源分析：
   业务库 MySQL：用户表、订单表、商品表
   埋点日志：页面浏览、点击、加购
   第三方数据：广告投放、物流

3. 数仓分层设计：
   ODS：源表原样同步
   DWD：清洗后的用户行为事实表 + 订单事实表
        + 用户维度表 + 商品维度表 + 时间维度表
   DWS：用户日活宽表、商品日销宽表
   ADS：运营看板所需的高聚合结果表

4. ETL 流程：
   离线 T+1：每天凌晨跑 Spark SQL，加工昨天的数据
   实时：Kafka → Flink → ClickHouse（实时大屏）

5. 关键指标计算：
   日活 = COUNT(DISTINCT user_id) WHERE login_date = today
   留存率 = 次日登录的 / 首日登录的
   转化率 = 下单的 / 访问的
```

### 题2：有一个 1TB 的大表，查询很慢，怎么优化？

```
1. 看查询模式：什么 SQL 最慢？
2. 建索引：高频查询条件建索引
3. 分区：按日期分区，查询只扫相关分区
4. 分桶：Join 的 Key 做分桶
5. 列式存储：ORC / Parquet 替代文本
6. 压缩：Snappy 压缩
7. 物化视图 / 预计算：常见聚合结果提前算好
8. 数据生命周期：冷数据归档，热数据保留
9. Hive 调优参数：MapJoin、并行度、小文件合并
```

### 题3：数据倾斜怎么处理？

```
现象：某个 Task 处理的数据量远大于其他 Task，导致整个 Job 卡住

常见原因：
  - GROUP BY 时某个 Key 的数据特别多（如北京用户占 60%）
  - JOIN 时某个关联 Key 特别多（如热门商品）

解决方案：
  1. 加盐打散：给倾斜 Key 加随机后缀 → 先局部聚合 → 再去后缀全局聚合
     例：user_id 变 user_id_0, user_id_1, ..., user_id_9
  
  2. 两阶段聚合：先随机分桶聚合一次，再全局聚合
  
  3. MapJoin：小表放内存广播，避免 Shuffle Join
  
  4. 过滤：如果倾斜的是无效数据（如 null 值），直接过滤掉
  
  5. 调参：增大 reduce 并行度
```

### 题4：如何处理数据延迟（上游数据没按时到）？

```
1. 监控 + 告警：Airflow 调度检测，上游延迟就发告警
2. 重试机制：配置重试次数和间隔
3. 降级方案：用上一周期的数据先顶上，等数据到了再回刷
4. SLA 管理：和上游约定交付时间，超时向上追责
5. 数据质量报告：标记"数据可能不完整"
```

### 题5：设计实时数仓，需求是业务大盘实时展示 GMV 和订单量

```
架构：
  业务库 MySQL → Canal 订阅 binlog → Kafka
  → Flink 实时消费：
      ① 订单宽表（Join 用户、商品信息）
      ② 窗口聚合（每分钟 GMV = SUM(order_amount)）
  → 写入 ClickHouse
  → BI 工具（DataV / Grafana）实时刷新大盘

分层：
  ODS：Kafka Topic 原始数据
  DWD：Flink 清洗后的订单事实表
  DWS：Flink 窗口聚合后的分钟级 GMV
  ADS：ClickHouse 中的看板表
```

---

# 第十三篇：非技术问题 / 行为面试

---

## ⭐ 13.1 必须准备的 8 个问题

### Q1: 自我介绍（已涵盖，见第一篇）

### ⭐ Q2: 你的优点是什么？

> 我认为自己有三个核心优势：
>
> 第一，**实践能力强**。我不是纯理论型的人，网络水军项目从数据采集到最终报告，整个 Pipeline 是我一行行代码写出来的，不是纸上谈兵。
>
> 第二，**学习速度快**。项目中需要 BERT 和 GNN，我快速自学并成功应用，最终让 F1 提升了 45%。
>
> 第三，**对数据工作有热情**。我做特征工程时，发现了数据中的很多有意思的模式，比如水军总在凌晨批量作业。这种从数据中挖掘规律的成就感让我很享受。

### ⭐ Q3: 你的缺点是什么？

> 我的主要不足是**大数据组件（Kafka、Flink、Hive）的实战经验还比较欠缺**。我目前对这些技术的理解主要来自学习和文档，还没有在生产环境中使用过。
>
> 但我已经在弥补这个不足：我系统学习了它们的工作原理，理解了 Kafka 在数据采集层的角色、Flink 做实时流处理的核心概念、Hive 的分区和分桶优化。入职后我会尽快通过实际项目把理论转化为实战能力。以我的学习能力，我有信心在 1-2 个月内达到独立开发的水平。

### ⭐ Q4: 为什么选择数据开发（而不是算法/前端）？

> 我在做网络水军项目时发现，比起调模型参数，**我更享受构建数据处理流程的过程**——把原始数据一步步转化成干净、结构化、可用的数据资产。这个过程中的 ETL 设计、性能优化、数据质量保障让我更有成就感。
>
> 而且我认为**数据开发是数据价值链条的基础环节**——没有高质量的数据，再好的算法也无用武之地。我希望在数据工程这个方向上深耕，成为数据基础设施的建设者。

### ⭐ Q5: 为什么选择启明星辰？

> 首先，启明星辰在信息安全领域非常专业，安全数据的实时性、准确性要求远超普通业务数据，这对数据开发能力是很好的锻炼和挑战。
>
> 其次，安全数据的敏感性决定了它对数据治理、权限管控有很高的要求，这能让我学到在普通业务场景中学不到的工程实践。
>
> 第三，我非常认可安全行业的价值——保护用户和企业的数据安全是很有意义的事情。

### ⭐ Q6: 你的职业规划？

> **短期（1-2 年）**：扎实掌握企业级数据开发技术栈（Hive、Kafka、Flink、Spark、数据治理），成为能独立负责数据仓库模块开发的工程师，把"理论能力"转化为"生产级能力"。
>
> **中期（3-5 年）**：向**数据架构**方向发展，能独立设计完整的数据仓库架构和实时数仓方案，对数据模型、ETL 流程、数据质量保障有体系化的方法论。
>
> **长期（5 年+）**：成为数据领域的专家，能从业务视角出发，规划数据平台的建设方向，带领团队交付有业务价值的数据产品。

### ⭐ Q7: 你遇到过的最大挑战？怎么解决的？

> 最大挑战是网络水军项目中的**类别严重不平衡问题**。正常用户和水军比例是 10:1，直接训练的话模型会"偷懒"，把所有评论都判为正常也有 90% 准确率，但这完全没意义。
>
> 我的解决思路是**三层组合策略**：
> 第一层在数据层面，用 SMOTETomek 采样，既扩充水军样本又清理边界噪声；
> 第二层在模型层面，设类别权重告诉模型"水军的代价是正常用户的 10 倍"；
> 第三层在评估层面，不以默认的 0.5 为阈值，而是遍历 0.3~0.7 找到 F1 最优的阈值。
>
> 最终 F1 从 0.53 提升到 0.84，证明三层策略是有效的。
>
> 这个经历也让我深刻理解了"数据质量决定模型上限"这个道理——如果不处理不平衡问题，后面的模型选型、参数调优都是白费力气。

### ⭐ Q8: 如果和同事意见不同，你怎么处理？

> 首先，我会**先理解对方的出发点**——是技术方案有分歧，还是对需求的解读不同？我会让对方充分表达他的思考，而不是急着反驳。
>
> 然后，**用数据和事实说话**。如果是技术方案分歧，我会分别列出两种方案的优缺点（性能、可维护性、开发周期），用数据验证而不是凭感觉争论。如果是需求理解不同，我会回溯到业务方的原始需求文档，以需求为准。
>
> 最后，如果还是无法达成一致，**我会尊重更有经验的人的意见**，但同时请求在实施过程中设置检验点——如果发现方案确实有问题，及时调整。团队目标是一致的，分歧只是路径不同。

---

## 13.2 其他可能会被问的问题

**Q: 你实习/工作中最有成就感的一件事？**
> （结合项目回答）网络水军项目中，当我发现加入 BERT 特征后 F1 从 0.58 跳到 0.84 的那一刻，非常有成就感。因为这意味着模型真的"理解"了语义，而不只是在数感叹号。

**Q: 你怎么学习新技术的？**
> 我一般三步走：第一，快速了解"是什么、为什么、怎么用"，通过官方文档和教程；第二，动手做一个 Demo，哪怕是最简单的 Hello World，亲手跑通比看十遍文档有效；第三，理解核心原理，知道底层是怎么实现的。比如学 BERT 时，我先用 Sentence-BERT 跑通语义向量提取，再去看 Transformer 和 Self-Attention 的原理。

**Q: 如果让你重做水军项目，会怎么改进？**
> 第一，工程化升级：把 pandas 单机方案改成 PySpark，支持分布式处理更大数据量。
> 第二，实时化改造：加 Kafka 接入实时评论流，用 Flink 做实时特征计算，上线实时检测。
> 第三，MLOps 自动化：用 Airflow 调度定期重训练，MLflow 管理实验和模型版本，监测数据漂移。

**Q: 你还有什么想问我的？（反问）**
> 1. "请问这个岗位日常工作中，离线数仓和实时数仓的工作量占比大概是怎样的？"
> 2. "团队目前主要用哪些大数据组件？技术栈是怎样的？"
> 3. "入职后，在前 3 个月里，您希望我在哪些方面尽快上手？"
> 4. "团队目前在数据开发方面遇到的最大技术挑战是什么？"

---

# 第十四篇：面试速记卡片

---

## 卡片 1：SQL 窗口函数速查

```
ROW_NUMBER() OVER (PARTITION BY a ORDER BY b)  → 去重/TopN
RANK() OVER (...)                              → 排名（会跳号）
DENSE_RANK() OVER (...)                        → 排名（不跳号）
SUM(col) OVER (ORDER BY date)                  → 累计求和
LAG(col, 1) OVER (ORDER BY date)               → 环比
LAG(col, 12) OVER (ORDER BY date)              → 同比
```

## 卡片 2：数仓分层速记

```
ADS → 应用层，BI 取数，直接可用
DWS → 汇总层，按主题轻度聚合
DWD → 明细层，清洗后的干净事实
ODS → 贴源层，原样保留业务数据
```

## 卡片 3：Kafka 核心速记

```
Producer → Topic → Partition → Consumer
Offset = 消费位置（断点续传）
不丢：acks=all + 多副本 + 手动 Commit
有序：同 Partition 有序，按 Key Hash 分区保证同类有序
```

## 卡片 4：Flink 核心速记

```
原生流处理，毫秒级
Event Time + Watermark = 处理乱序数据
Window：滚动（不重叠）、滑动（可重叠）、会话（活动间隙）
Checkpoint = 容错快照（自动），Savepoint = 手动快照
```

## 卡片 5：Hive 核心速记

```
SQL → 翻译成 MapReduce/Tez/Spark 执行
分区 = 按目录切（dt='2024-01-01'）
分桶 = 按文件切（user_id Hash 取模）
ORC + Snappy = 查询快 + 存储省
```

## 卡片 6：集成学习速记

```
Bagging：并行，投票，降方差 → RandomForest
Boosting：串行，纠错，降偏差 → XGBoost/LightGBM/CatBoost
Stacking：多模型输出当输入，元模型综合学
```

## 卡片 7：类别不平衡处理

```
数据层：SMOTE 过采样 + Tomek Links 清边界
模型层：class_weight / scale_pos_weight 加权
评估层：阈值优化（不以 0.5 为准）
```

## 卡片 8：XGBoost 五大优势

```
① 正则化 L1+L2 → 防过拟合
② 二阶泰勒展开 → 损失优化更精确
③ 自动处理缺失值 → 学习最优方向
④ 特征级并行 → 分裂搜索快
⑤ 列采样 → 防止过拟合
```

## 卡片 9：ETL 速记

```
E = Extract   抽取（从源系统拿数据）
T = Transform 转换（清洗、计算、标准化）
L = Load      加载（写入目标系统）

增量抽取：时间戳增量 / 自增ID增量 / CDC(binlog)
```

## 卡片 10：数据质量六维度

```
完整性（该有的有吗？）
准确性（数据对吗？）
一致性（各处一样吗？）
及时性（按时到了吗？）
唯一性（有重复吗？）
有效性（格式对吗？）
```

---

# 第十五篇：面试前 3 天冲刺清单

---

## 每天必做

- [ ] **大声朗读自我介绍**（1分30秒版），直到不看稿也能流利说出
- [ ] **大声朗读项目介绍**（3分钟版），直到不看稿也能流利说出
- [ ] **默写核心数字**：12万、10:1、0.53→0.84、95.7%
- [ ] **手写 3 道 SQL 题**（窗口函数、连续问题、累计求和）
- [ ] **过一遍速记卡片**（14张卡片，5分钟即可）

## 面试前一天

- [ ] **模拟面试**：找一个人（或对着镜子）完整讲一遍自我介绍 + 项目
- [ ] **查看启明星辰官网**，了解最新产品、新闻、业务方向
- [ ] **准备 2 个反问问题**
- [ ] **确认面试时间、地点/链接**

## 面试当天早上

- [ ] 再看一遍核心数字表
- [ ] 再看一遍速记卡片
- [ ] 再大声朗读一遍自我介绍和项目介绍
- [ ] 放松心态——你已经准备得很充分了

---

> **最后的建议**：
>
> 1. **自信从容**。面试官看的不只是技术，更是一个人面对问题的态度。遇到不会的，坦诚承认 + 展示学习思路 > 乱编瞎说。
>
> 2. **主动引导**。面试官不知道你最擅长什么，把话题往网络水军项目上引——那是你最有话语权的地方。
>
> 3. **用数字说话**。"效果很好"不如"F1 从 0.53 提升到 0.84"。
>
> 4. **展示思考过程**。面试官问一个你不会的技术，你可以说"我目前没有实战经验，但我的理解是...，我的学习计划是..."。这比直接说"我不会"好 100 倍。
>
> **祝你面试顺利！**
