# 面试重点强化版 —— 考前 1 小时速通

> 这是最精简的版本，只保留**最容易考到、最容易忘记、最能加分**的内容。  
> 面试前 1 小时完整过一遍。

---

## 🔴 一、自我介绍（必须滚瓜烂熟）

> 面试官您好，我叫李欣昱，应聘数据开发。我从三个方面介绍自己：
>
> **第一，完整的 ETL 和数据 Pipeline 经验。** 在网络水军检测项目中，独立完成了从数据清洗、特征工程、图建模到集成学习的全流程。F1 从 **0.53 提升到 0.84**，准确率 **95.7%**。这个流程本质上就是轻量化 ETL。
>
> **第二，数据建模思维。** 项目中构建了用户-商品二部图模型发现水军团伙，用 PageRank + Louvain 做团伙分析。能将业务需求抽象为数据模型。
>
> **第三，快速学习能力。** 从零学了 BERT 和 GNN 并成功融入方案，BERT 让 F1 直接提升 45%。对 Kafka、Flink、Hive 已系统学习原理，入职后能快速上手。

---

## 🔴 二、项目 1 分钟版

> 我做了个电商水军检测系统。12 万条评论，水军只占 10%，严重不平衡。
>
> 流程分几步：第一，特征工程，从文本、行为、时间、评分提取 20+ 特征；第二，无监督检测，三种方法投票发现异常；第三，图网络分析，构建二部图用 Louvain 发现团伙、PageRank 找头目；第四，集成学习，SMOTETomek 采样 + 四模型 Voting/Stacking；第五，BERT 语义增强——**效果最大**，F1 从 0.58 跳到 0.84。
>
> 最终 F1 **0.84**，准确率 **95.7%**。

---

## 🔴 三、核心数字（面试中主动报）

```
12万条评论    10:1 不平衡      20+ 手工特征
384维 BERT    4模型集成        3层策略（采样+权重+阈值）
8步Pipeline   F1: 0.53→0.84   准确率 95.7%
```

---

## 🔴 四、SQL 抢分题（3 道必考）

### ① 每个用户最近一次订单（窗口函数 TopN）

```sql
SELECT user_id, order_amount
FROM (
    SELECT *, ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY order_date DESC) rn
    FROM orders
) t WHERE rn = 1;
```

### ② 连续 3 天登录（连续问题）

```sql
-- 日期减去序号，连续日期的差值相同
SELECT DISTINCT user_id FROM (
    SELECT user_id,
           DATE_SUB(login_date, INTERVAL ROW_NUMBER() OVER (
               PARTITION BY user_id ORDER BY login_date) DAY) diff
    FROM (SELECT DISTINCT user_id, login_date FROM logins) t1
) t2 GROUP BY user_id, diff HAVING COUNT(*) >= 3;
```

### ③ SQL 执行顺序（口述）

```
FROM → JOIN → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT
```

---

## 🔴 五、数仓核心（必背 4 句话）

```
1. 四层：ODS(贴源) → DWD(明细) → DWS(汇总) → ADS(应用)

2. OLTP(数据库) vs OLAP(数仓)：
   数据库：交易、最新数据、增删改查、3NF
   数仓：分析、历史数据、批量写入、维度建模

3. 星型模型：事实表+维度表直接关联，冗余换速度
   雪花模型：维度表再拆分，规范但多层JOIN慢

4. SCD Type2（最常用）：新增一行+时间戳，保留完整历史
```

---

## 🔴 六、大数据速记（每个组件只记关键词）

### Kafka

```
Producer → Topic(Partition) → Consumer
四大优势：解耦 + 削峰 + 高吞吐 + 可回溯
不丢三板斧：acks=all / 多副本≥2 / 手动Commit
有序：同 Partition 内有序，不同 Partition 无序
```

### Flink

```
原生流处理，毫秒级延迟（Spark Streaming 是微批，秒级）
核心五词：EventTime + Watermark + Window + Checkpoint + Savepoint
```

### Hive

```
= SQL翻译器（HiveQL→MapReduce/Spark）
分区=按目录切(日期)，分桶=按文件切(Join Key)
优化：分区裁剪、ORC+Snappy、MapJoin
```

---

## 🔴 七、六大必问知识点（一句话回答）

| 问题 | 一句话答案 |
|------|----------|
| 数据倾斜怎么解？ | 加盐打散→局部聚合→去盐全局聚合；或 MapJoin 广播小表 |
| Kafka 为什么快？ | 顺序写磁盘 + 零拷贝 + PageCache + 批量发送 |
| XGBoost 为什么好？ | 正则化 + 二阶导 + 缺失值自处理 + 列采样 + 并行 |
| Bagging vs Boosting？ | Bagging 并行投票降方差(RF)；Boosting 串行纠错降偏差(XGB) |
| 精确率 vs 召回率？ | 精确率=抓的人里多少是对的；召回率=坏人里抓了多少 |
| SMOTE 原理？ | 在少数类样本的 K 近邻之间随机插值，生成"人造水军" |

---

## 🔴 八、弱点应对（必然会问）

### Java 不行？

> 主力语言是 Python，Java 不擅长。但能看懂接口代码、和 Java 后端协作过。Flink 支持 Java，我了解基本语法。我的优势是学习快——BERT 从零到应用只用很短时间，Java 同样可以快速上手。

### Kafka/Flink/Hive 没经验？

> 确实没有生产实战经验。但我系统学习了原理：Kafka 是消息队列负责数据采集层，Flink 是流处理引擎毫秒级延迟，Hive 是把 SQL 翻译成 MapReduce 跑在 Hadoop 上。我有 Python 实现 ETL 的完整经验，核心逻辑是相通的，换成这些工具就是换了个实现方式。

---

## 🔴 九、非技术应答模板

### 为什么选数据开发？

> 比起调模型参数，我更享受构建数据处理流程——把原始数据变成干净可用的资产。数据开发是数据价值链条的基础，我在这方面有实践有热情。

### 职业规划？

> 1-2 年扎实掌握企业级技术栈（Hive/Kafka/Flink），能独立开发；3-5 年向数据架构方向发展。

### 反问面试官？

> "团队目前离线数仓和实时数仓的占比大概是怎样的？""入职前 3 个月，您希望我优先补齐哪些技能？"

---

## 🔴 十、急救模板 —— 遇到不会的技术

> "关于 **[技术点]**，我目前的理解是 **[一句话核心原理]**。虽然更深入的细节和实战经验我还欠缺，但我的学习能力比较强——项目中从零学到了 BERT 和 GNN 并成功应用。如果有需要我可以快速补齐。我理解**[你其实会的相关东西]**。"

---

## 🔴 十一、最后一页速记卡（面试前 5 分钟看）

```
⭐ F1: 0.53 → 0.84  准确率: 95.7%   12万条  10:1  8步
⭐ SQL: ROW_NUMBER + PARTITION BY + ORDER BY
⭐ 执行顺序: FROM → JOIN → WHERE → GROUP BY → HAVING → SELECT → ORDER BY
⭐ 数仓: ODS → DWD → DWS → ADS
⭐ Kafka: Producer → Topic(Partition) → Consumer
⭐ Flink: EventTime + Watermark + Window + Checkpoint
⭐ Hive: 分区(日期目录) + 分桶(Key取模) + ORC+Snappy
⭐ 数据倾斜: 加盐打散 → 两阶段聚合
⭐ 精确率(抓的对) vs 召回率(抓的全) → F1找平衡
⭐ 自我介绍第一句: "我叫李欣昱，应聘数据开发，有三个核心优势"
```

---

> **最后提醒**：自信 > 完美。不会就说"我理解核心原理是...，具体细节还在学"，比沉默好一万倍。  
> **你的王牌项目是网络水军检测，任何时候都可以往它上面引。**
